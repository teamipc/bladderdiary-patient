---
plan_id: "07-01"
phase: 7
wave: 1
depends_on: []
files_modified:
  - src/components/onboarding/OnboardingFlow.tsx
requirements_addressed: ["DTUX-04"]
autonomous: true
---

# Plan 07-01: OnboardingFlow desktop redesign — typography bumps, age/unit/date input scaling, back-pill 44px hit-target fix, Enter-advance keyboard contract, focus-visible migration on non-Button inputs

<objective>
Apply the locked Phase 7 design contract from `07-UI-SPEC.md` §"Onboarding Flow — Per-Step Specs" + §"Shared step chrome" to `src/components/onboarding/OnboardingFlow.tsx` (316 lines). Single file modification; no other surfaces touched in this plan (file-ownership rule — Plan 07-02 owns summary, Plan 07-03 owns ExportActions).

This plan closes DTUX-04 (onboarding editorial desktop layout) by:
1. Bumping all 3 step `<h2>` headlines from `text-2xl` to `text-2xl md:text-3xl` (lines 107, 149, 210). UI-SPEC Q1 default-locked at `md:text-3xl` (not `text-4xl`) per hierarchy + Boomer-safe override 7 rationale; the `md:text-4xl` fallback is a Phase 8 visual-qa remediation if the bumped headlines feel under-confident at 1440px.
2. Scaling the age input (line 116-129) to `md:w-32 md:text-4xl` so the "30" stops swimming in 1920px whitespace — the user's stated problem #1 from CONTEXT `<specifics>`.
3. Widening the unit toggle buttons (lines 157-181) from `flex-1 max-w-[160px] py-6` to `flex-1 max-w-[160px] md:max-w-[200px] py-6 md:py-8` — both `mL` and `oz` buttons identical class diff.
4. Scaling the date input (lines 217-231) `py-3 text-base` → `py-3 md:py-4 text-base md:text-lg`.
5. Bumping BOTH back-pill buttons (lines 196, 279) from `min-h-[40px]` to `min-h-[44px]` — this is THE ONLY ACCEPTED MOBILE DIFF in Phase 7, equivalent to Phase 5's NavLink correctness fix per Boomer-safe override 1 (the 44px hit-target floor). UI-SPEC §"Hit-target audit" + §"Mobile invariants per step" both explicitly accept this carve-out.
6. Adding Enter-to-advance handlers (DTUX-04 keyboard deliverable) on each step's inner `<div key="stepN">` container via `onKeyDown`. Pattern locked in UI-SPEC §"Keyboard contract" — exclude TEXTAREA target, ignore shiftKey, preventDefault, then mirror the existing Next/Confirm onClick logic. Step 1 → advance to step 2 if `isAgeValid`. Step 2 → always advance to step 3 (volumeUnit defaults to 'mL'). Step 3 → call `handleConfirm()`.
7. Migrating focus rings from `focus:` → `focus-visible:` on the 4 non-Button interactive controls that don't inherit from Phase 6's Button primitive: age input (line 125), date input (line 227), the 2 unit-toggle `<button>` elements (lines 160, 173), and the 2 back-pill `<button>` elements (lines 196, 279). The Phase 5/6 spec is `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-{white|surface}` — `white` for white-fill controls (inputs), `surface` for body-bg controls (back pills, unit toggles).

UI-SPEC source coverage:
- §"Shared step chrome" → "Headline typography bump — LOCKED" → applies to all 3 step h2s
- §"Shared step chrome" → "Back-pill buttons — HIT-TARGET BUMP LOCKED" → lines 196 + 279 diff
- §"Shared step chrome" → "Keyboard contract (NEW — DTUX-04 deliverable)" → onKeyDown per-step
- §"Shared step chrome" → "Focus-visible ring spec" + §"Per-element override" → migration matrix
- §"Step 1 (age) — LOCKED" → age input class diff (lines 116-129)
- §"Step 2 (units) — LOCKED" → toggle button class diff (lines 156-182)
- §"Step 3 (date + timezone) — LOCKED" → date input class diff (lines 217-231); timezone-change pill NO CHANGE (deferred to Phase 8); 3-day preview card NO CHANGE
- §"Locale-safe illustration decision — DEFER" → UI-SPEC Q2 locked default = no illustration; this plan does NOT add one
- §"Mobile invariants per step" → exhaustive matrix this plan must preserve

Open questions addressed:
- Q1 — Default locked `md:text-3xl` for h2 typography bumps. Documented in task notes.
- Q2 — Default locked NO illustration (no asset added; no `hidden md:block` chrome). Documented in task notes.
- Q4 — Timezone-change pill hit target: NOT TOUCHED this phase (deferred to Phase 8 visual-qa per UI-SPEC Q4 default). Documented in task notes.

Out of scope for this plan:
- summary/page.tsx (Plan 07-02)
- ExportActions.tsx (Plan 07-03)
- The Phase 8 visual-qa cross-locale matrix (Plan 07-04 + Phase 8)
- Adding new i18n strings (UI-SPEC §"i18n + RTL Contract" verifies ZERO new keys — all existing onboarding copy already in messages/*.json)
- Restart-onboarding ConfirmDialog flow (UI-SPEC + CONTEXT.md both confirm: no restart affordance exists today; nothing to wire)
- BottomSheet changes (timezone picker inherits Phase 6 transformation; Phase 7 makes ZERO BottomSheet changes per UI-SPEC §"Mobile invariants per step")

Constraints from CLAUDE.md + memory:
- `'use client'` preserved on OnboardingFlow.tsx (Zustand consumer — already present)
- TypeScript strict; no `as any`; no `@ts-ignore`
- All existing logical CSS preserved (`ms-`/`pe-`/`ps-`/`rtl:scale-x-[-1]`)
- All Phase 5/6 inherited behavior preserved (Container `narrow`, step-dot wayfinding, animate-step-in-{left,right} animations, autoFocus on age input)
- Mobile (<768px) HARD CONSTRAINT — ZERO new mobile carve-outs except the 2 back-pill +4px hit-target correctness fix
- No new dependencies; no new state management; no class composition libraries
- ChevronLeft / ChevronRight icons + lucide-react imports preserved
</objective>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingFlow.tsx — headline typography bumps + age input scaling + Step 1 focus-visible migration (Step 1 region: lines 104-144)</name>

  <read_first>
    - src/components/onboarding/OnboardingFlow.tsx (FULL — 316 lines; identify lines 90 Container wrap, 92-102 step dot indicator, 104-144 Step 1 block, 107 h2, 116-129 age input, 132-142 Next CTA, 184 spacing wrapper)
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Shared step chrome" → "Headline typography bump — LOCKED" (lines ~146-156), §"Shared step chrome" → "Focus-visible ring spec" (lines ~75-96), §"Step 1 (age) — LOCKED" (lines ~229-287)
    - .planning/phases/07-onboarding-summary-surfaces/07-CONTEXT.md §"Onboarding layout strategy at md+" (locked decisions about headline bump magnitude + age input proportions)
    - .planning/phases/05-layout-foundation-appshell-chrome/05-UI-SPEC.md (Container `narrow` variant = max-w-2xl = 672px — context for "swimming in whitespace" geometry)
    - src/components/layout/Container.tsx (verify `narrow` variant unchanged; do NOT re-wrap)
    - src/components/ui/Button.tsx (verify `size="lg"` already min-h-[52px], primary variant focus-visible inherited — NO Phase 7 Button changes here)
  </read_first>

  <files>src/components/onboarding/OnboardingFlow.tsx</files>

  <action>
    Edit Step 1 region of OnboardingFlow.tsx (existing lines 104-144 — `{step === 1 && ( ... )}`). Three className changes, NO logic/structure changes:

    1. **Line 107 h2 typography bump:**
       Replace the className string `text-2xl font-bold text-ipc-950 mb-2 text-balance` with `text-2xl md:text-3xl font-bold text-ipc-950 mb-2 text-balance`. The `md:text-3xl` is locked default per UI-SPEC Q1; defer the `md:text-4xl` fallback to Phase 8 visual-qa if 1440px feels under-confident.

    2. **Lines 116-129 age input — width + type-size + focus-visible migration:**
       Replace the full className string (currently `"w-28 text-center text-3xl font-bold text-ipc-950 bg-white/60 border-2 border-ipc-200/50 rounded-2xl py-3 outline-none focus:border-ipc-500/60 focus:ring-2 focus:ring-ipc-200/30 transition-all placeholder:text-ipc-200 placeholder:text-xl [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"`) with the Phase 7 form:
         `w-28 md:w-32 text-center text-3xl md:text-4xl font-bold text-ipc-950 bg-white/60 border-2 border-ipc-200/50 rounded-2xl py-3 outline-none focus-visible:border-ipc-500/60 focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white transition-all placeholder:text-ipc-200 placeholder:text-xl [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none`
       Three diffs: `w-28` → `w-28 md:w-32`; `text-3xl` → `text-3xl md:text-4xl`; `focus:border-ipc-500/60 focus:ring-2 focus:ring-ipc-200/30` → `focus-visible:border-ipc-500/60 focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white`. The `transition-all`, `[appearance:textfield]`, and webkit-spin-button suppressions are preserved unchanged. `autoFocus` on line 128 preserved. `inputMode="numeric"`, `min={18}`, `max={120}` preserved. The `py-3` is NOT bumped per UI-SPEC §"Step 1 → Why not also bump `py-3`" — the natural line-height grows the input to ~66px at md+ which is already well above the 44px floor.

    3. **Preserve everything else in Step 1 untouched:** the wrapping `<div key="step1" className={\`w-full text-center ${animClass}\`}>` (line 106 — `onKeyDown` will be added in Task 2 of this plan), the `<p className="text-sm text-ipc-500 mb-8">` subtitle (line 110 — NO CHANGE per UI-SPEC), the `<div className="flex justify-center mb-8">` wrapper around the input (line 114), the `<div className="max-w-xs mx-auto">` wrapper around Next (line 132), the Button `size="lg"` + onClick logic (lines 133-141 — NO change; Button primitive owns its hover + focus-visible from Phase 6).

    Use `Edit` (not `Write`) and operate ONLY on the Step 1 region for this task.

    **Mobile-invariant explicitly preserved:** every NEW class is `md:`-prefixed (`md:w-32`, `md:text-3xl`, `md:text-4xl`). The `focus-visible:` migration is keyboard-only and does not fire on touch. Mobile rendering at 375px is byte-equivalent to today.
  </action>

  <acceptance_criteria>
    - `grep -n 'text-2xl md:text-3xl' src/components/onboarding/OnboardingFlow.tsx` returns at least 1 match on or near line 107 (Step 1 headline).
    - `grep -n 'w-28 md:w-32' src/components/onboarding/OnboardingFlow.tsx` returns exactly 1 match in the age-input className.
    - `grep -n 'text-3xl md:text-4xl' src/components/onboarding/OnboardingFlow.tsx` returns exactly 1 match in the age-input className.
    - `grep -n 'focus-visible:border-ipc-500/60' src/components/onboarding/OnboardingFlow.tsx` returns exactly 1 match in the age-input className.
    - `grep -nv '^#' src/components/onboarding/OnboardingFlow.tsx | grep -c 'focus:border-ipc-500/60'` for the age input returns 0 (no remaining `focus:` form on that input).
    - `grep -n 'focus-visible:ring-offset-white' src/components/onboarding/OnboardingFlow.tsx` returns exactly 1 match (age input — date input will add a 2nd in Task 3).
    - `grep -n 'autoFocus' src/components/onboarding/OnboardingFlow.tsx` still returns the line on the age input (line ~128) — autoFocus PRESERVED.
    - `npx tsc --noEmit` exits 0 (TypeScript strict mode clean — no `as any`, no `@ts-ignore`).
    - `npx eslint src/components/onboarding/OnboardingFlow.tsx` exits 0.
    - Mobile invariant grep: `grep -E "(ml-|mr-|pl-|pr-|left-|right-)" src/components/onboarding/OnboardingFlow.tsx | grep -v 'rtl:scale-x-\[-1\]'` returns NO NEW physical CSS introduced by this task (the existing `ms-`/`pe-`/`ps-` logical CSS is preserved).
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/onboarding/OnboardingFlow.tsx && grep -c 'text-3xl md:text-4xl' src/components/onboarding/OnboardingFlow.tsx | grep -E '^[1-9]' && grep -c 'w-28 md:w-32' src/components/onboarding/OnboardingFlow.tsx | grep -E '^[1-9]'</automated>
  </verify>

  <done>
    Step 1 region (lines 104-144) has h2 typography bumped via `md:text-3xl`, age input scaled to `md:w-32 md:text-4xl`, focus migrated to `focus-visible:` with Phase 5/6 ring spec on white offset. autoFocus preserved. TypeScript + ESLint clean. Mobile classes at 375px byte-equivalent (the `md:`-prefixed additions are inert below 768px).
  </done>
</task>

<task type="auto">
  <name>Task 2: OnboardingFlow.tsx — Step 2 unit-toggle widening + focus-visible, Step 2 + Step 3 back-pill 44px hit-target bump + focus-visible, Enter-advance keyboard contract on all 3 steps</name>

  <read_first>
    - src/components/onboarding/OnboardingFlow.tsx (FULL — re-confirm line numbers may have shifted by ≤0 lines from Task 1 since Task 1 only changed class strings, not line counts; verify Step 2 still spans ~lines 146-205, Step 3 still spans ~lines 207-288)
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Step 2 (units) — LOCKED" (lines ~291-330), §"Shared step chrome" → "Back-pill buttons — HIT-TARGET BUMP LOCKED" (lines ~170-183), §"Shared step chrome" → "Keyboard contract (NEW — DTUX-04 deliverable)" (lines ~185-225)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Keyboard contract" (Phase 6 reference pattern for Enter-advance with TEXTAREA exclusion) — verify the exclusion guard convention used in Phase 6 forms
    - src/components/diary/LogDrinkForm.tsx (any one Phase 6 form — quick reference for the existing Enter-advance pattern Phase 6 established; verify the `onKeyDown` is on the form/step wrapper and the TEXTAREA check uses `target.tagName === 'TEXTAREA'`)
  </read_first>

  <files>src/components/onboarding/OnboardingFlow.tsx</files>

  <action>
    Three locked changes to OnboardingFlow.tsx — operate via `Edit` (multiple edits, single file):

    **Change 1 — Step 2 unit toggle widening + focus-visible (lines 156-182):**
    Both `mL` (lines 157-168) and `oz` (lines 170-181) toggle buttons currently use the same conditional className. Replace each button's className-string with the Phase 7 form:
      `flex-1 max-w-[160px] md:max-w-[200px] py-6 md:py-8 rounded-2xl border-2 transition-all active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface ${volumeUnit === 'mL' ? 'border-ipc-500 bg-ipc-50 ring-2 ring-ipc-200/50' : 'border-ipc-200/50 bg-white/60 hover:border-ipc-300'}`
    The `oz` button uses `volumeUnit === 'oz'` ternary. Diffs: `max-w-[160px]` → `max-w-[160px] md:max-w-[200px]`; `py-6` → `py-6 md:py-8`; append `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface` (offset-surface because the toggle sits on body bg-surface).

    **Change 2 — Both back-pill buttons (lines 196 + 279) — hit-target bump + focus-visible:**
    Replace each back-pill button's className-string (currently `inline-flex items-center justify-center gap-1 px-4 min-h-[40px] rounded-full text-sm font-semibold text-ipc-700 bg-white border border-ipc-200 hover:bg-ipc-50 active:scale-[0.97] transition-all`) with the Phase 7 form:
      `inline-flex items-center justify-center gap-1 px-4 min-h-[44px] rounded-full text-sm font-semibold text-ipc-700 bg-white border border-ipc-200 hover:bg-ipc-50 active:scale-[0.97] transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface`
    Two diffs per button: `min-h-[40px]` → `min-h-[44px]` (THE accepted Phase 7 mobile diff — Boomer-safe override 1 correctness fix; +4px tap-target grows at all viewports, equivalent to Phase 5 NavLink); append the same `focus-visible:` ring spec used on toggles. Apply the EXACT same diff to both line 196 (back to Step 1) and line 279 (back to Step 2).

    **Change 3 — Enter-advance keyboard contract on all 3 step containers:**
    Each step's outer `<div key="stepN">` currently has only `className={\`w-full text-center ${animClass}\`}` (Steps 1+2; Step 3 has `${animClass}`). Add an `onKeyDown` handler to each:

      Step 1 (line ~106) — add: `onKeyDown={(e) => { const target = e.target as HTMLElement; if (target.tagName === 'TEXTAREA') return; if (e.key !== 'Enter' || e.shiftKey) return; e.preventDefault(); if (isAgeValid) { track('onboarding_age', { age: ageNum }); goForward(2); } }}`
      Step 2 (line ~148) — add: `onKeyDown={(e) => { const target = e.target as HTMLElement; if (target.tagName === 'TEXTAREA') return; if (e.key !== 'Enter' || e.shiftKey) return; e.preventDefault(); track('onboarding_unit', { unit: volumeUnit }); goForward(3); }}`
      Step 3 (line ~209) — add: `onKeyDown={(e) => { const target = e.target as HTMLElement; if (target.tagName === 'TEXTAREA') return; if (e.key !== 'Enter' || e.shiftKey) return; e.preventDefault(); handleConfirm(); }}`

    Pattern mirrors UI-SPEC §"Keyboard contract" exactly. The TEXTAREA exclusion is defensive consistency with Phase 6 — none of the onboarding steps contain a textarea, but the guard is preserved for future-proofing. The `e.preventDefault()` prevents the native browser Enter behavior on the focused input (which could submit a form or insert a newline depending on context). Each handler then mirrors the existing Next/Confirm button onClick logic — Step 1's `goForward(2)` matches line 134's CTA; Step 2's `goForward(3)` matches line 186's CTA; Step 3's `handleConfirm()` matches line 270's Confirm CTA.

    **Note on the timezone-change pill (line 239-246):** NO CHANGE this phase. UI-SPEC §"Step 3 → Timezone-change pill" explicitly defers it to Phase 8 visual-qa (the pill is ~28px tall, below the 44px floor, but it's a secondary tap target on a step where the primary CTAs are all ≥44px). Document in commit message that this is deferred per UI-SPEC Q4.

    **Note on locale-safe illustration:** NO ASSET ADDED this phase. UI-SPEC §"Locale-safe illustration decision — DEFER" + Q2 both lock the default at type-driven only.

    **Mobile-invariant verification:** every NEW class addition uses `md:` prefix EXCEPT the `min-h-[40px]` → `min-h-[44px]` bump on the 2 back pills (the explicit accepted +4px diff at all viewports — see UI-SPEC §"Hit-target audit"). The Enter-advance onKeyDown handlers are keyboard-only events; touch users never fire them. Focus-visible rings are keyboard-only.
  </action>

  <acceptance_criteria>
    - `grep -c 'max-w-\[160px\] md:max-w-\[200px\]' src/components/onboarding/OnboardingFlow.tsx` returns 2 (mL toggle + oz toggle).
    - `grep -c 'py-6 md:py-8' src/components/onboarding/OnboardingFlow.tsx` returns 2 (mL + oz toggles).
    - `grep -c 'min-h-\[44px\]' src/components/onboarding/OnboardingFlow.tsx` returns 2 (the 2 back-pill buttons; the bump replaced 2 occurrences of `min-h-[40px]`).
    - `grep -c 'min-h-\[40px\]' src/components/onboarding/OnboardingFlow.tsx` returns 0 (no remaining 40px floor on back pills).
    - `grep -c 'focus-visible:ring-offset-surface' src/components/onboarding/OnboardingFlow.tsx` returns 4 (2 unit toggles + 2 back pills; date input gets ring-offset-white in Task 3).
    - `grep -c 'onKeyDown=' src/components/onboarding/OnboardingFlow.tsx` returns 3 (one per step container).
    - `grep -c "if (e.key !== 'Enter' || e.shiftKey) return" src/components/onboarding/OnboardingFlow.tsx` returns 3 (one per Enter-advance handler).
    - `grep -c "target.tagName === 'TEXTAREA'" src/components/onboarding/OnboardingFlow.tsx` returns 3 (one TEXTAREA exclusion per handler).
    - `npx tsc --noEmit` exits 0.
    - `npx eslint src/components/onboarding/OnboardingFlow.tsx` exits 0.
    - Vitest existing suite still passes: `npx vitest run` exits 0 (Phase 7 introduces NO new vitest tests; only verifies existing tests pass).
    - Logical-CSS guard: `grep -nE "(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|left-|right-)[0-9]" src/components/onboarding/OnboardingFlow.tsx | grep -v 'rtl:scale-x-\[-1\]' | grep -v 'absolute left-3.5'` should return ONLY the pre-existing `absolute left-3.5` Calendar-icon position on line ~220 (which is inside `dir`-aware parent — flagged for Phase 8 to convert to `start-3.5` if visual-qa surfaces a problem; NOT a Phase 7 fix per UI-SPEC scope).
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/onboarding/OnboardingFlow.tsx && [ "$(grep -c 'min-h-\[44px\]' src/components/onboarding/OnboardingFlow.tsx)" = "2" ] && [ "$(grep -c 'min-h-\[40px\]' src/components/onboarding/OnboardingFlow.tsx)" = "0" ] && [ "$(grep -c 'onKeyDown=' src/components/onboarding/OnboardingFlow.tsx)" = "3" ] && npx vitest run --reporter=dot</automated>
  </verify>

  <done>
    Step 2 unit toggles widen to `md:max-w-[200px] md:py-8`. Both back-pill buttons bump to `min-h-[44px]` (Boomer-safe override 1 correctness fix — only Phase 7 mobile diff). Focus-visible rings added on 2 unit toggles + 2 back pills (4 total, all with `ring-offset-surface`). Enter-advance handlers wired on all 3 step containers, each with TEXTAREA exclusion + shiftKey guard + preventDefault + the step-appropriate forward action (goForward(2) / goForward(3) / handleConfirm()). Existing vitest suite passes unchanged. Timezone-change pill + locale-safe illustration explicitly NOT modified per UI-SPEC Q2 + Q4 default-locked positions.
  </done>
</task>

<task type="auto">
  <name>Task 3: OnboardingFlow.tsx — Step 3 date input scaling + focus-visible migration; final headline bumps on Step 2 + Step 3 h2 (catch-up if not done in Task 1)</name>

  <read_first>
    - src/components/onboarding/OnboardingFlow.tsx (FULL — re-confirm Step 2 h2 on line ~149, Step 3 h2 on line ~210, date input on lines ~217-231)
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Step 3 (date + timezone) — LOCKED" (lines ~333-368), §"Mobile invariants per step" (lines ~389-399), §"Focus-visible ring spec" (the Phase 7 migration table)
    - .planning/phases/07-onboarding-summary-surfaces/07-CONTEXT.md §"Onboarding layout strategy at md+" → "Locked: Headline typography bumps via responsive class only"
  </read_first>

  <files>src/components/onboarding/OnboardingFlow.tsx</files>

  <action>
    Two locked changes — operate via `Edit`:

    **Change 1 — Step 2 h2 + Step 3 h2 headline bumps (lines ~149 + ~210):**
    Both lines currently use `text-2xl font-bold text-ipc-950 mb-2 text-balance` (Step 2) and `text-2xl font-bold text-ipc-950 mb-1.5 text-balance` (Step 3 — note `mb-1.5` not `mb-2`). Apply the SAME `text-2xl md:text-3xl` bump Task 1 applied to Step 1's h2:
      Line 149 (Step 2 unitTitle): `text-2xl` → `text-2xl md:text-3xl` (preserve `font-bold text-ipc-950 mb-2 text-balance` tail)
      Line 210 (Step 3 dateTitle): `text-2xl` → `text-2xl md:text-3xl` (preserve `font-bold text-ipc-950 mb-1.5 text-balance` tail — `mb-1.5` is intentional Step-3 spacing, NOT a typo)

    **Change 2 — Step 3 date input scaling + focus-visible migration (lines 217-231):**
    Replace the date input's className-string (currently `ps-10 pe-4 py-3 text-base font-semibold text-ipc-950 bg-white/60 border-2 border-ipc-200/50 rounded-2xl outline-none focus:border-ipc-500/60 focus:ring-2 focus:ring-ipc-200/30 transition-all`) with the Phase 7 form:
      `ps-10 pe-4 py-3 md:py-4 text-base md:text-lg font-semibold text-ipc-950 bg-white/60 border-2 border-ipc-200/50 rounded-2xl outline-none focus-visible:border-ipc-500/60 focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white transition-all`
    Three diffs: `py-3` → `py-3 md:py-4`; `text-base` → `text-base md:text-lg`; `focus:border-ipc-500/60 focus:ring-2 focus:ring-ipc-200/30` → `focus-visible:border-ipc-500/60 focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white`. The `ps-10 pe-4` logical CSS is preserved (RTL-safe). `<input type="date">` semantics preserved. The Calendar icon on line ~220 (`absolute left-3.5 top-1/2 -translate-y-1/2`) is NOT touched this phase — UI-SPEC explicitly defers the `left-3.5` → `start-3.5` migration to Phase 8 visual-qa.

    **Preserve everything else in Step 3 untouched:** the date-input wrapper `<div className="flex justify-center mb-2.5">` and `<div className="relative">` (lines 218-219), the Calendar icon (line 220), the timezone-change pill (lines 239-246 — NO CHANGE per UI-SPEC Q4), the 3-day preview card (lines 250-266 — NO CHANGE per UI-SPEC §"3-day preview card"), the Confirm & Start Button `size="lg"` (lines 269-275 — NO CHANGE; inherits Phase 6 Button focus-visible + 52px floor), the back pill (line 279 — already modified in Task 2).
  </action>

  <acceptance_criteria>
    - `grep -c 'text-2xl md:text-3xl' src/components/onboarding/OnboardingFlow.tsx` returns 3 (one per step h2).
    - `grep -c 'py-3 md:py-4' src/components/onboarding/OnboardingFlow.tsx` returns 1 (the date input).
    - `grep -c 'text-base md:text-lg' src/components/onboarding/OnboardingFlow.tsx` returns 1 (the date input).
    - `grep -c 'focus-visible:ring-offset-white' src/components/onboarding/OnboardingFlow.tsx` returns 2 (age input from Task 1 + date input from Task 3).
    - `grep -c 'focus:border-ipc-500/60' src/components/onboarding/OnboardingFlow.tsx` returns 0 (no remaining old `focus:` rings on inputs).
    - `grep -c 'focus:ring-ipc-200/30' src/components/onboarding/OnboardingFlow.tsx` returns 0 (no remaining old `focus:` ring color tokens).
    - `grep -c 'min-h-\[44px\]' src/components/onboarding/OnboardingFlow.tsx` STILL returns 2 (unchanged from Task 2).
    - `grep -c 'min-h-\[40px\]' src/components/onboarding/OnboardingFlow.tsx` STILL returns 0 (unchanged from Task 2).
    - `npx tsc --noEmit && npx eslint src/components/onboarding/OnboardingFlow.tsx` both exit 0.
    - `npx vitest run` exits 0 (no regressions in existing unit tests).
    - Production build clean: `npm run build` exits 0 (verifies Tailwind 4 `@theme inline` consumes all the new `md:`-prefixed classes; verifies the static export still generates).
    - Logical-CSS preservation check: the date input's `ps-10 pe-4` (logical) is preserved; no new physical `pl-`/`pr-` introduced.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/onboarding/OnboardingFlow.tsx && [ "$(grep -c 'text-2xl md:text-3xl' src/components/onboarding/OnboardingFlow.tsx)" = "3" ] && [ "$(grep -c 'focus-visible:ring-offset-white' src/components/onboarding/OnboardingFlow.tsx)" = "2" ] && [ "$(grep -c 'focus:border-ipc-500/60' src/components/onboarding/OnboardingFlow.tsx)" = "0" ] && npm run build</automated>
  </verify>

  <done>
    All 3 step h2 headlines now bump `text-2xl md:text-3xl`. Step 3 date input scales `py-3 md:py-4 text-base md:text-lg` with `focus-visible:` ring on white offset. Calendar icon, timezone-change pill, 3-day preview card all preserved unchanged (per UI-SPEC §"Step 3" + Q4). The full Phase 7 OnboardingFlow contract is implemented: 3 h2 bumps + age input scaling + 2 unit toggles widened + date input scaled + 2 back pills bumped to 44px + 3 Enter-advance handlers + 4 focus-visible migrations on non-Button inputs. Production build clean. Vitest clean. Mobile (<768px) renders byte-equivalent except the 2 back-pill +4px hit-target accepted carve-out.
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "At 1440px in EN, Step 1's `<h2>` renders at 30px (`md:text-3xl`) not 24px"
    - "At 1440px in EN, Step 1's age input is 128px wide and the typed number renders at 36px"
    - "At 1440px in EN, Step 2's mL+oz toggle buttons are each ≤200px max width with 32px vertical padding"
    - "At 1440px in EN, Step 3's date input has 16px vertical padding and 18px type"
    - "At ALL widths, both back-pill buttons render with a minimum 44px tap height"
    - "Tab to the age input in Step 1 and press Enter (with valid age 18-120) — the step advances to Step 2"
    - "Tab to anywhere in Step 2 and press Enter — the step advances to Step 3"
    - "Tab to the date input in Step 3 and press Enter — handleConfirm fires (onboarding completes)"
    - "Keyboard-focus the age input (via Tab, not click) — a visible `ring-ipc-500` ring with white offset appears"
    - "Touch-tap the age input on a mobile device — NO ring appears (focus-visible is keyboard-only)"
    - "At 375px (iPhone SE) in all 6 locales, OnboardingFlow renders byte-equivalent to pre-Phase-7 EXCEPT for the 2 back-pill buttons being 4px taller (accepted correctness fix)"
  artifacts:
    - path: "src/components/onboarding/OnboardingFlow.tsx"
      provides: "3 step h2 with md:text-3xl, age input md:w-32 md:text-4xl, unit toggles md:max-w-[200px] md:py-8, date input md:py-4 md:text-lg, 2 back pills min-h-[44px], 3 onKeyDown Enter-advance handlers, 4 focus-visible ring migrations (age input, date input, 2 unit toggles, 2 back pills = 6 total focus-visible insertions; Button-primitive CTAs inherit from Phase 6)"
      contains: ["text-2xl md:text-3xl", "w-28 md:w-32", "text-3xl md:text-4xl", "max-w-[160px] md:max-w-[200px]", "py-6 md:py-8", "py-3 md:py-4", "text-base md:text-lg", "min-h-[44px]", "focus-visible:ring-ipc-500", "onKeyDown=", "isAgeValid", "handleConfirm()"]
  key_links:
    - from: "Step 1 onKeyDown handler"
      to: "isAgeValid check + goForward(2)"
      via: "Enter key press with age 18-120 typed"
      pattern: "if \\(isAgeValid\\) \\{[^}]+goForward\\(2\\)"
    - from: "Step 2 onKeyDown handler"
      to: "track + goForward(3)"
      via: "Enter key press (volumeUnit defaults to mL so always valid)"
      pattern: "track\\('onboarding_unit'.*goForward\\(3\\)"
    - from: "Step 3 onKeyDown handler"
      to: "handleConfirm() (line ~70-74)"
      via: "Enter key press"
      pattern: "handleConfirm\\(\\)"
    - from: "Age input className"
      to: "Tailwind 4 @theme inline tokens"
      via: "md:w-32 + md:text-4xl + focus-visible:ring-ipc-500 token references"
      pattern: "md:w-32.*md:text-4xl"
</must_haves>

<verification>
  - TypeScript: `npx tsc --noEmit` exits 0.
  - ESLint: `npx eslint src/components/onboarding/OnboardingFlow.tsx` exits 0.
  - Vitest: `npx vitest run` exits 0 (the existing 427/427 baseline from Phase 6 should hold).
  - Production build: `npm run build` exits 0; `out/` directory generated with valid HTML.
  - Class-count verifications:
    - `grep -c 'text-2xl md:text-3xl' src/components/onboarding/OnboardingFlow.tsx` returns 3 (one per step h2)
    - `grep -c 'min-h-\[44px\]' src/components/onboarding/OnboardingFlow.tsx` returns 2 (back pills only — Button-primitive CTAs already 52px and not counted)
    - `grep -c 'min-h-\[40px\]' src/components/onboarding/OnboardingFlow.tsx` returns 0
    - `grep -c 'onKeyDown=' src/components/onboarding/OnboardingFlow.tsx` returns 3 (one per step)
    - `grep -c 'focus-visible:ring-ipc-500' src/components/onboarding/OnboardingFlow.tsx` returns 6 (age + date + 2 toggles + 2 back pills — confirms the Phase 7 migration of 6 controls)
    - `grep -c 'focus:border-ipc-500' src/components/onboarding/OnboardingFlow.tsx` returns 0 (no remaining old `focus:` rings on the modified controls)
  - Logical-CSS guard (mobile + RTL invariant): `grep -nE "(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|right-)[0-9]" src/components/onboarding/OnboardingFlow.tsx | grep -v 'rtl:scale-x-\[-1\]'` returns NO NEW matches added by Phase 7; the pre-existing `absolute left-3.5` on the Calendar icon (line ~220) is the only pre-existing physical CSS and is OUT of Phase 7 scope (UI-SPEC defers it to Phase 8).
  - Mobile invariant grep: `grep -c 'w-28 md:w-32' src/components/onboarding/OnboardingFlow.tsx` returns 1 — confirms the `md:` prefix; no bare `w-32` class introduced.
  - Phase 7 surface-specific accepted carve-out: the 2 back-pill `min-h-[40px]` → `min-h-[44px]` is THE ONLY mobile diff in Plan 07-01.
</verification>

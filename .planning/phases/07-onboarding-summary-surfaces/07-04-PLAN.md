---
plan_id: "07-04"
phase: 7
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - e2e/phase7-onboarding-summary.spec.ts
requirements_addressed: ["DTUX-04", "DTUX-05"]
autonomous: false
---

# Plan 07-04: New Playwright spec `e2e/phase7-onboarding-summary.spec.ts` covering onboarding 3-step desktop layout (6 locales x 3 widths) + summary H1/typography (6 locales x 3 widths) + export-button hover smoke test + back-pill 44px hit-target verification; aggregate physical-CSS guard across the 3 modified files; SEO regression check on out/{locale}.html; human-verify checkpoint with screenshots

<objective>
Verify ALL 5 Phase 7 success criteria from ROADMAP §"Phase 7: Onboarding + Summary surfaces" hold against the actual built site, across the 6-locale × {375px / 768px / 1280px} viewport matrix specified in 07-UI-SPEC.md §"Verification Checklist (for Phase 8)".

The 5 ROADMAP success criteria for Phase 7:
1. At ≥ 768px, OnboardingFlow.tsx step layouts use a confidently-sized content column with proportioned step indicator and primary CTA — the age input is sized for desktop hit-target conventions (not the same compact mobile size), the timezone picker is laid out for desktop reading.
2. At ≥ 768px, `/en/summary` (and locale equivalents) renders the metric cards in a multi-column grid (target: 3-up at md, 5-up at lg for the 5 IPC metrics) instead of stacked. **CORRECTED INTERPRETATION per UI-SPEC §"Container variant decision":** the 5 IPC clinical metrics (24HV/NPi/AVV/MVV/NBC) live ONLY in CSV/PDF exports, NOT in-page. What Phase 7 actually delivers is the existing 3-stat top grid (voidCount/drinkCount/daysComplete) with `md:px-4 md:py-5` tile-padding bump — Plan 07-02. This verification asserts the FLAT-tile boundary (Design DNA axis 4) holds AND the H1 + section H2 typography bumps land.
3. At ≥ 768px, `ExportActions.tsx` (CSV / PDF / Share buttons) has visible hover states on each button (color shift or subtle elevation), respects a reasonable max-width (does not stretch full-bleed), and has clear focus rings. **Plan 07-03 implements this** via `md:hover:-translate-y-px md:transition-all md:duration-150` (1px lift; Boomer-safe override 2) + Button-primitive variant bg-color hover (inherited) + `md:max-w-2xl md:mx-auto` outer wrap + Phase-6-Button-primitive `focus-visible:ring-ipc-500` (inherited).
4. RTL (Arabic) onboarding + summary verify clean — column layouts mirror correctly via logical properties, no `ml-`/`mr-` regressions.
5. Mobile (<768px) onboarding + summary unchanged; daily walkthrough still green.

The plan adds a NEW Playwright spec (`e2e/phase7-onboarding-summary.spec.ts`) that runs against a LOCAL static-export build served by `npx --yes serve out -l 4173 --no-clipboard` (same pattern as Phase 5's 05-07 plan + Phase 6's 06-11 plan, both checker-PASSED).

**CRITICAL — Playwright invocation pattern (corrected for installed Playwright 1.59.1):** the `--test-match` CLI flag does NOT exist (only `--grep`/`--grep-invert` which filter test TITLE not file path). Phase 5's executor added a `PW_TEST_MATCH` env-var hook to `playwright.config.ts:30` (`const VERIFICATION_MATCH = process.env.PW_TEST_MATCH;`) which adds a one-off "verification" project when the env var is set. **This Phase 7 plan reuses that env-var pattern**: every Playwright invocation uses `PW_TEST_MATCH='phase7-onboarding-summary\.spec\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line`.

**CRITICAL — out/ path convention (corrected for Next.js 16 + next-intl localePrefix 'as-needed'):** the static export emits HTML at `out/en.html`, `out/fr.html`, `out/es.html`, `out/pt.html`, `out/zh.html`, `out/ar.html` (each locale as a TOP-LEVEL .html file — NOT `out/{locale}/index.html`). This is the pattern Phase 5's 05-07 plan locked in (the executor discovered it during gap-closure and the pattern carried into Phase 6's 06-11 plan). The SEO regression check + cross-locale HTML asserts MUST use the `out/{locale}.html` paths.

**CRITICAL — H1 grep on out/{locale}.html is INFORMATIONAL not BLOCKER:** per Phase 5 lesson (the H1 hydration race surfaced in Phase 5 verification — pre-existing concern), the `<h1>` element on the summary page is rendered client-side by the React 19 + Zustand hydration. Phase 5 + Phase 6 verification specs both treat the H1 grep on the static HTML as informational (the H1 appears AFTER hydration, not in the pre-hydration static HTML). Phase 7's H1 bump is a className change on an existing `<h1>` — the change WILL appear in the hydrated DOM (which the Playwright tests assert via `page.locator('h1')` after page.goto), but it may NOT appear in the static `out/en.html` if Next.js 16's RSC partial-prerender flushes the summary page's interactive client component below the hydration boundary. The SEO regression check verifies canonical/hreflang/lang attribute/JSON-LD are preserved; the H1 count is informational.

The plan also runs:
- **Aggregate physical-CSS grep guard** across the 3 modified files (OnboardingFlow.tsx, summary/page.tsx, ExportActions.tsx) — verifies NO `ml-`/`mr-`/`pl-`/`pr-`/`left-`/`right-` physical CSS introduced by Phase 7 (the pre-existing `absolute left-3.5` on the Calendar icon in OnboardingFlow.tsx line ~220 is explicitly excluded — UI-SPEC §"Step 3" defers it to Phase 8; the pre-existing `ml-2` in DaySummaryCard.tsx line 41 is OUT of Phase 7 scope per UI-SPEC §"DaySummaryCard").
- **FLAT-tile boundary grep guard** on summary/page.tsx + summary subcomponents — verifies NO `shadow-xl`/`shadow-lg`/`shadow-md`/`ring-1 ring-black` elevation on the 3-stat tiles, top-standout card, observations card, day-summary cards, or coming-back card (Design DNA axis 4 medical-rigor boundary). Verifies `grep -c 'shadow-' src/app/[locale]/summary/page.tsx src/components/summary/SummaryObservations.tsx src/components/export/DaySummaryCard.tsx` returns 0 across these files.
- **2 back-pill 44px hit-target verification** — Playwright assertion `await expect(backPill).toHaveCSS('min-height', '44px')` on both Step 2 + Step 3 back pills in EN at 1280px (the +4px hit-target bump is the ONLY accepted Phase 7 mobile diff).
- **Enter-advance keyboard verification** — Playwright tests in EN + AR (RTL invariant) that pressing Enter on Step 1 (with valid age 50) advances to Step 2; on Step 2 advances to Step 3; on Step 3 completes onboarding (page transitions to /diary/day/1). Mirrors Phase 6's pattern from 06-11.
- **Focus-visible ring smoke test** — Playwright `page.keyboard.press('Tab')` to focus the age input, then `await expect(ageInput).toHaveCSS('outline-style', 'none')` AND `await expect(ageInput).toHaveCSS('box-shadow', /ipc-500|rgb\(149,\s*90,\s*20\)/)` or similar — verifying the `focus-visible:ring-ipc-500` lights up.
- **Export-button hover smoke test** — Playwright `await pdfButton.hover()` followed by `await expect(pdfButton).toHaveCSS('transform', /matrix\(1, 0, 0, 1, 0, -1\)|translateY/)` — verifying the `md:hover:-translate-y-px` lift fires at 1280px (it should NOT fire at 375px because the `md:` prefix gates it).
- **i18n parity check** — Phase 7 introduces ZERO new keys, but verify the existing 6-locale parity holds: `node .claude/scripts/article-i18n-completeness.sh` or equivalent `jq` keys-count diff across `messages/{en,fr,es,pt,zh,ar}.json`. Per CLAUDE.md §"Configuration": "Stop hook + pre-commit hook enforce article translation coverage" — the same hooks apply to UI strings.
- **Mobile screenshot diff** at 375px in en/ar (the highest-risk locales for RTL physical-CSS leaks) for: onboarding Step 1 + Step 2 + Step 3 OPEN states (the 2 back-pill +4px bump WILL show as a small diff on Step 2 + Step 3; this is the accepted Phase 7 mobile diff) + summary page (NO diff expected — neither Plan 07-02 nor Plan 07-03 introduces a mobile diff).
- **Human-verify checkpoint** with screenshots in 6 locales × 3 widths × the 3 surfaces (onboarding Step 1/2/3, summary, export buttons hovered).

This plan is `autonomous: false` because of the human-verify checkpoint. Wave 2 — depends on Plans 07-01, 07-02, 07-03.

UI-SPEC source coverage:
- §"Verification Checklist (for Phase 8)" → the per-surface verification matrix this spec automates
- §"Mobile Invariants Summary (DO NOT REGRESS)" → the 5-row matrix the spec asserts
- §"i18n + RTL Contract" → the logical-CSS guard + RTL invariants the spec verifies
- §"Accessibility (Phase 7 scope)" → the focus-ring visibility + heading hierarchy assertions
- §"6-Pillar Pre-Check for Phase 8" → the typography / color / spacing / hierarchy / interaction / accessibility checkpoints (Pillar 5 — Interaction — most directly enforced by the spec's hover-lift smoke test + Enter-advance test)

Open questions addressed:
- (No new UI-SPEC Q's pertain to verification; the spec exercises the locked defaults from Q1/Q3/Q5 — `md:text-3xl` headlines, `default` Container variant, `md:px-4 md:py-5` tile padding — and the deferred-but-not-broken positions of Q2 [no illustration] and Q4 [timezone-change pill unchanged].)

Out of scope for this plan:
- Implementation of the 3 modified files (Plans 07-01, 07-02, 07-03)
- Phase 8 full cross-locale visual-qa matrix (the visual-qa skill runs the matrix; Plan 07-04 is a focused regression-gate spec, not a comprehensive matrix)
- Fixing pre-existing `ml-2` in DaySummaryCard.tsx line 41 (Phase 8)
- Fixing pre-existing `absolute left-3.5` on Calendar icon in OnboardingFlow.tsx (Phase 8)
- Browser zoom resilience tests at 100/150/200% (Phase 8 visual-qa)
- Real iOS Safari ITP / Android Chrome touch-hover differential testing (Phase 8 visual-qa)
- Surfacing the H1 hydration race as a Phase 7 blocker (pre-existing concern from Phase 5 verification; H1 grep on static HTML is informational)

Constraints from CLAUDE.md + memory:
- Test file naming: `kebab-case.spec.ts` per CLAUDE.md §"Naming Patterns" — `phase7-onboarding-summary.spec.ts` matches
- E2E tests use `from '@playwright/test'` (NOT vitest)
- No `as any`, no `@ts-ignore`
- zsh quoting — when running shell commands that reference `[locale]` paths, single-quote the paths or escape brackets (planner verified at write-time; the spec uses `path.resolve('out', \`${locale}.html\`)` template literals which avoid quoting issues in Node)
- All 6 locales remain at parity (verified via Phase 7's `i18n parity check` task; Phase 7 introduces ZERO new keys so this is a regression check on the pre-existing parity)
- Mobile-pristine HARD CONSTRAINT — Phase 5's First Principle (this plan is the verification gate for the 3 modified files)
- The `walkthrough_findings.md` (per CLAUDE.md memory `walkthrough_findings`) — after Phase 7 ships, the daily walkthrough should produce NO new findings; this plan does NOT replace the daily walkthrough, it gates the Phase 7 PR before merge
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create e2e/phase7-onboarding-summary.spec.ts covering 6-locale x 3-width matrix for onboarding desktop layout + summary typography + export-button hover smoke test + Enter-advance keyboard + back-pill 44px hit target</name>

  <read_first>
    - playwright.config.ts (FULL — verify `testMatch: TEST_MATCH` line ~39 + `const VERIFICATION_MATCH = process.env.PW_TEST_MATCH;` line ~30; this is the pattern Phase 5 + Phase 6 specs already use)
    - e2e/walkthrough.spec.ts (reference pattern: how to drive onboarding via the FAB, how to use locale-aware selectors via `e2e/helpers/messages.ts`, how to capture screenshots, how to seed store state, how to set viewport)
    - e2e/phase5-chrome.spec.ts (FULL — the Phase 5 verification spec — pattern reference for the local-build verification harness, the `LOCALES`/`VIEWPORTS` constants, the `localePath` helper)
    - e2e/phase6-keyboard.spec.ts (FULL — Phase 6 verification spec — pattern reference for Enter-advance tests, ZH initial-focus tests, focus-ring assertions, the `seedActiveDay1` fixture pattern)
    - e2e/helpers/fixtures.ts (verify `STORE_KEY` constant + any existing `buildSeedState` helper)
    - e2e/helpers/messages.ts (verify locale-specific selector helpers — e.g., labels.exportSavePdf(locale), labels.next(locale), labels.back(locale))
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Verification Checklist (for Phase 8)" FULL section (lines ~940-973) — every checkbox is a candidate test assertion
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Mobile Invariants Summary" FULL section (lines ~696-716) — the matrix the spec verifies
    - .planning/phases/07-onboarding-summary-surfaces/07-UI-SPEC.md §"Accessibility (Phase 7 scope)" → "Focus-ring visibility audit" (the 5 ring colors the spec smoke-tests)
    - .planning/ROADMAP.md §"Phase 7" → "Success Criteria" (the 5 ROADMAP criteria the spec asserts)
    - src/components/onboarding/OnboardingFlow.tsx (FULL — post-Plan-07-01; verify the Phase 7 className additions are present before the spec runs)
    - src/app/[locale]/summary/page.tsx (FULL — post-Plan-07-02)
    - src/components/export/ExportActions.tsx (FULL — post-Plan-07-03)
  </read_first>

  <files>e2e/phase7-onboarding-summary.spec.ts</files>

  <action>
    Create a new Playwright spec at `e2e/phase7-onboarding-summary.spec.ts`. The spec is INDEPENDENT of the daily-walkthrough config. It serves the local static export from `out/` on port 4173 and walks the 6-locale × 3-width matrix for onboarding + summary + export-button verification.

    **Spec file structure (top-of-file):**

    ```ts
    /**
     * Phase 7 onboarding + summary + export-button verification spec.
     *
     * Covers the 6-locale x 3-width Phase 7 verification matrix:
     *   - 6 locales: en / fr / es / pt / zh / ar
     *   - 3 viewports: 375px (mobile invariant), 768px (md activates), 1280px (full desktop)
     *
     * Per-surface assertions:
     *   - OnboardingFlow.tsx (Plan 07-01): H2 typography bump, age input md:w-32 md:text-4xl,
     *     unit toggle md:max-w-[200px] md:py-8, date input md:py-4 md:text-lg, 2 back pills 44px,
     *     Enter-advance keyboard contract, focus-visible rings on age input + date input + unit
     *     toggles + back pills
     *   - summary/page.tsx (Plan 07-02): H1 md:text-4xl + md:px-0, section H2s md:text-xl,
     *     3-stat tiles md:px-4 md:py-5, Container variant stays 'default' (no widening), FLAT
     *     tile boundary preserved (no shadow / no ring on tiles)
     *   - ExportActions.tsx (Plan 07-03): md:hover:-translate-y-px lift smoke test, md:max-w-2xl
     *     md:mx-auto wrap, Button primitive variant hover inherited, focus-visible inherited
     *
     * Cross-surface assertions:
     *   - Aggregate physical-CSS grep guard across the 3 modified files
     *   - FLAT-tile boundary grep across summary surfaces (Design DNA axis 4)
     *   - i18n parity check (Phase 7 introduces zero new keys; regression guard on pre-existing parity)
     *   - Mobile screenshot diff at 375px in EN + AR (the +4px back-pill bump is the only accepted Phase 7 mobile diff)
     *
     * Screenshots written to test-results/phase7-onboarding-summary/ for the human-verify checkpoint.
     *
     * To run:
     *   npm run build
     *   npx --yes serve out -l 4173 --no-clipboard &
     *   PHASE7_BASE_URL=http://localhost:4173 \
     *     PW_TEST_MATCH='phase7-onboarding-summary\.spec\.ts' \
     *     npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line
     *
     * IMPORTANT - Playwright 1.59.1 invocation:
     *   The --test-match CLI flag does NOT exist; use PW_TEST_MATCH env var which
     *   playwright.config.ts:30 reads and converts to a one-off verification project.
     *   This pattern was locked in by Phase 5's 05-07 plan and reused by Phase 6's 06-11.
     */

    import { test, expect, type Page } from '@playwright/test';
    import { mkdirSync, readFileSync } from 'node:fs';
    import { resolve } from 'node:path';
    import { STORE_KEY } from './helpers/fixtures';

    const LOCALES = ['en', 'fr', 'es', 'pt', 'zh', 'ar'] as const;
    type Locale = (typeof LOCALES)[number];

    const VIEWPORTS = [
      { name: 'mobile-375', width: 375, height: 800 },
      { name: 'desktop-768', width: 768, height: 900 },
      { name: 'desktop-1280', width: 1280, height: 900 },
    ] as const;

    const BASE_URL = process.env.PHASE7_BASE_URL ?? 'http://localhost:4173';
    const SCREENSHOT_DIR = resolve('test-results/phase7-onboarding-summary');
    mkdirSync(SCREENSHOT_DIR, { recursive: true });

    function localePath(locale: Locale, path: string): string {
      const cleanPath = path.startsWith('/') ? path : `/${path}`;
      return `/${locale}${cleanPath === '/' ? '' : cleanPath}`;
    }
    ```

    **Test groups (organize using `test.describe`):**

    1. **`describe('Onboarding desktop layout matrix')`** — for each locale × viewport (18 combinations):
       - Navigate to `/${locale}/` (LandingContent.tsx; verify OnboardingFlow renders on first visit by NOT seeding any localStorage state)
       - At 1280px in EN: assert `await expect(page.locator('h2').first()).toHaveCSS('font-size', '30px')` (verifies `md:text-3xl` = 30px)
       - At 375px in EN: assert `await expect(page.locator('h2').first()).toHaveCSS('font-size', '24px')` (verifies mobile-pristine `text-2xl`)
       - At 1280px in EN: assert `await expect(page.locator('input[type=number]')).toHaveCSS('width', '128px')` (verifies `md:w-32`)
       - At 1280px in EN: assert `await expect(page.locator('input[type=number]')).toHaveCSS('font-size', '36px')` (verifies `md:text-4xl`)
       - Type a valid age (50), click Next, navigate to Step 2
       - At 1280px in EN: assert `await expect(page.locator('button:has-text("mL")')).toHaveCSS('max-width', '200px')` (verifies `md:max-w-[200px]`)
       - At 1280px in EN: assert `await expect(page.locator('button:has-text("mL")')).toHaveCSS('padding-top', '32px')` (verifies `md:py-8`)
       - Verify the BACK PILL on Step 2: `await expect(page.getByRole('button', { name: /back/i })).toHaveCSS('min-height', '44px')` — the +4px hit-target bump (THE ONLY accepted Phase 7 mobile diff)
       - At 375px in EN: assert the same back-pill is min-h-[44px] (the bump applies at ALL viewports — accepted +4px diff)
       - Navigate to Step 3 (click Next)
       - At 1280px in EN: assert `await expect(page.locator('input[type=date]')).toHaveCSS('padding-top', '16px')` (verifies `md:py-4`)
       - At 1280px in EN: assert `await expect(page.locator('input[type=date]')).toHaveCSS('font-size', '18px')` (verifies `md:text-lg`)
       - Screenshot each step at each viewport (Step 1 / Step 2 / Step 3 × {mobile-375, desktop-768, desktop-1280}) for the human-verify checkpoint
       - Repeat for FR + ES + PT + ZH + AR (the AR run additionally verifies the wrapping `<div dir="rtl">` is present on the html element — sanity check on RTL inheritance)

    2. **`describe('Onboarding Enter-advance keyboard contract')`** — EN + AR only (RTL invariant verification per UI-SPEC §"Keyboard navigation"):
       - Navigate to `/${locale}/` (or `/`)
       - Focus the age input (it has `autoFocus`)
       - Type "50"
       - Press Enter: assert page is now showing Step 2 (`await expect(page.getByText(/mL/)).toBeVisible()` or similar)
       - Press Enter again: assert page is now showing Step 3 (date input visible)
       - Press Enter again: assert page navigates to `/diary/day/1` (the `handleConfirm()` completion path) — `await expect(page).toHaveURL(/.*diary\/day\/1$/)`
       - For AR: same flow; verify the RTL `dir="rtl"` on html element doesn't invert the Enter behavior (Enter still advances FORWARD, never backward)

    3. **`describe('Summary page typography')`** — for each locale × viewport (18 combinations):
       - Seed a COMPLETE 3-day diary state into localStorage (use existing `STORE_KEY` from helpers; minimal data: 3 days complete, bedtimes for all 3, voids on each day, drinks on each day) so the summary page renders the celebration branch (not the locked-state branch)
       - Navigate to `/${locale}/summary`
       - At 1280px in EN: assert `await expect(page.locator('h1')).toHaveCSS('font-size', '36px')` (verifies H1 `md:text-4xl`)
       - At 375px in EN: assert `await expect(page.locator('h1')).toHaveCSS('font-size', '24px')` (verifies mobile-pristine `text-2xl`)
       - At 1280px in EN: assert section H2s render at 20px (`md:text-xl`) — use `await expect(page.locator('h2').filter({ hasText: 'storyTitle-or-equivalent' }))` via the messages helper for the locale's actual translation
       - At 1280px in EN: assert the 3-stat tiles render with `padding-top: 20px` (verifies `md:py-5`) and `padding-left: 16px` (verifies `md:px-4`)
       - Assert FLAT-tile boundary: `await expect(stat1Tile).not.toHaveCSS('box-shadow', /^.+/)` (no shadow), `await expect(stat1Tile).not.toHaveCSS('outline', /^(?!none).+/)` (no outline ring) — use the negation form to assert no elevation
       - Container variant assertion: `await expect(page.locator('[class*="max-w-3xl"]')).toBeVisible()` AND `await expect(page.locator('[class*="max-w-5xl"]')).not.toBeAttached()` — verifies Container stays `default` (max-w-3xl), NOT widened to `wide` (max-w-5xl)
       - Screenshot the summary page at each viewport for each locale (18 screenshots) for human-verify

    4. **`describe('Export-button hover affordance smoke test')`** — EN only at 1280px (mouse-hover test):
       - Seed the complete 3-day diary state (reuse from describe 3)
       - Navigate to `/en/summary`
       - At 1280px, locate the PDF Button: `const pdfBtn = page.getByRole('button', { name: labels.exportSavePdf('en') });` (use messages helper)
       - Initial transform: `await expect(pdfBtn).toHaveCSS('transform', /none|matrix\(1, 0, 0, 1, 0, 0\)/)` (no transform applied; baseline state)
       - Hover: `await pdfBtn.hover()`
       - After hover: `await expect(pdfBtn).toHaveCSS('transform', /matrix\(1, 0, 0, 1, 0, -1\)|translateY/)` — verifies the `md:hover:-translate-y-px` lift fires (`-1px` translateY)
       - Verify the wrap is centered + capped: locate the outer wrapper div containing the buttons; `await expect(wrapper).toHaveCSS('max-width', '672px')` (= max-w-2xl) AND `await expect(wrapper).toHaveCSS('margin-left', /auto|.*\d+/)` (mx-auto centered)
       - At 375px: assert hover does NOT fire the lift (verify either via `mobileBtn.hover()` then assert transform stays at baseline because `md:` prefix gates it — this is the mobile-invariant proof)

    5. **`describe('Focus-visible ring smoke test')`** — EN at 1280px:
       - Navigate to `/en/` (onboarding Step 1)
       - Click somewhere neutral (e.g., page body), then press Tab to focus the age input
       - Assert `await expect(ageInput).toHaveCSS('box-shadow', /rgb\(149,\s*90,\s*20\)|ipc-500/)` — verifies the `focus-visible:ring-ipc-500` lights up on Tab focus
       - Pre-click the age input via mouse: assert NO ring (focus-visible is keyboard-only on most browsers — touch/mouse doesn't trigger). Skip this part if Playwright's mouse click triggers focus-visible (browser-dependent); document the deferral as Phase 8 visual-qa coverage.

    6. **`describe('SEO regression check')`** — read static `out/{locale}.html` files (NOT `out/{locale}/index.html` — Next.js 16 + next-intl `localePrefix: 'as-needed'` emits top-level locale HTML files per Phase 5's locked path convention):
       - For each locale: `const html = readFileSync(resolve('out', \`${locale}.html\`), 'utf8');`
       - Assert canonical link present: `expect(html).toMatch(/<link rel="canonical" href="https:\/\/myflowcheck.com\/?(?:${locale === 'en' ? '' : locale}\/?)?"/)`
       - Assert hreflang links present for all 6 locales: `for (const l of LOCALES) expect(html).toMatch(new RegExp(\`<link rel="alternate" hreflang="\${l}"\`));`
       - Assert `<html lang="${locale}">` and (for AR) `dir="rtl"`
       - Assert JSON-LD MedicalWebPage block present
       - H1 count assertion: **INFORMATIONAL** — `const h1Count = (html.match(/<h1\\b/g) || []).length;` — log `console.log(\`${locale}: H1 count = ${h1Count}\`);` but do NOT fail the test if H1 count is 0. Phase 5 + Phase 6 lessons: the H1 is rendered client-side after hydration; the static HTML may have H1 count 0 (the hydration race is a pre-existing concern, NOT a Phase 7 regression).

    7. **`describe('Aggregate physical-CSS grep guard')`** — Node-level (runs via `test.beforeAll` or as a single sync test, reads the 3 modified files via fs):
       - Read `src/components/onboarding/OnboardingFlow.tsx`, `src/app/[locale]/summary/page.tsx`, `src/components/export/ExportActions.tsx` via `readFileSync`
       - Regex: `/(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|left-|right-)[0-9]/gm` (excluding 'rtl:scale-x-[-1]' via second filter pass)
       - Allowlist the 1 pre-existing match: `absolute left-3.5` on OnboardingFlow.tsx line ~220 (Calendar icon position — UI-SPEC §"Step 3" defers to Phase 8)
       - Assert any matches NOT in the allowlist are 0
       - Print all matches found for debugging if the assertion fails

    8. **`describe('FLAT-tile boundary grep guard')`** — Design DNA axis 4 enforcement:
       - Read `src/app/[locale]/summary/page.tsx` + `src/components/summary/SummaryObservations.tsx` + `src/components/summary/DrinkVoidTimeline.tsx` + `src/components/export/DaySummaryCard.tsx`
       - Assert `grep -c 'shadow-' SUM(of all 4 files)` returns 0 — no elevation on in-page summary tiles
       - Assert `grep -c 'ring-1 ring-black' SUM(of all 4 files)` returns 0
       - Print findings if assertion fails

    9. **`describe('i18n parity check')`** — verify the 6 locale message files have key-set parity (Phase 7 introduces ZERO new keys; this is a regression guard on the pre-existing parity):
       - Read `messages/{en,fr,es,pt,zh,ar}.json`
       - Recursively flatten each into a sorted key-set
       - Assert all 6 locales have IDENTICAL key sets
       - Print any locales with extra/missing keys

    10. **`describe('Mobile invariant screenshot diff')`** — at 375px in EN + AR for: onboarding Step 1, Step 2, Step 3, summary page:
        - Take fresh screenshots at 375px and save to `test-results/phase7-onboarding-summary/mobile-{locale}-{surface}.png`
        - Document expected diffs:
          - Onboarding Step 2 + Step 3: the back-pill button is +4px taller (the accepted Phase 7 mobile diff)
          - Onboarding Step 1: NO diff
          - Summary page: NO diff in either EN or AR
        - The diff isn't fully automated in this plan; the human-verify checkpoint reviews the screenshots and confirms the diff matches expectations.

    **Test execution command (documented in the spec header comment):**
    `npm run build && (npx --yes serve out -l 4173 --no-clipboard &) && sleep 3 && PHASE7_BASE_URL=http://localhost:4173 PW_TEST_MATCH='phase7-onboarding-summary\\.spec\\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line`

    Note: the `&` background process + sleep is documented in the comment; the actual CI/local execution leaves the server cleanup to the developer (Phase 5 + Phase 6 specs followed the same pattern — manual `kill %1` after the test run).

    Use template literals + `path.resolve()` everywhere for filesystem paths to avoid zsh quoting issues with the `[locale]` bracket-path.

    Total LOC target: ~400-500 lines (similar to Phase 6's 06-11 plan output — large but bounded; the 10 describe blocks each are 30-50 lines).
  </action>

  <acceptance_criteria>
    - File `e2e/phase7-onboarding-summary.spec.ts` exists and is `>= 400` lines.
    - File imports `from '@playwright/test'` (not vitest): `grep -c "from '@playwright/test'" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File imports `STORE_KEY` from `./helpers/fixtures`: `grep -c "STORE_KEY" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 2 (import + usage).
    - File uses `LOCALES = ['en', 'fr', 'es', 'pt', 'zh', 'ar']`: `grep -c "'en', 'fr', 'es', 'pt', 'zh', 'ar'" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File uses 3 viewports including `375`, `768`, `1280`: `grep -cE "(375|768|1280)" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 3.
    - File contains at least 10 `describe(` blocks: `grep -c "test.describe(" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 10.
    - File asserts the 44px back-pill bump: `grep -c "min-height.*44px\\|'44px'" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File asserts the H1 36px bump: `grep -cE "'36px'|font-size.*36" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File asserts the export-button hover-lift: `grep -c "translateY\\|matrix(1, 0, 0, 1, 0, -1)" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File uses `out/${locale}.html` path convention (NOT `out/${locale}/index.html`): `grep -E "out.*locale.*\\.html|out.*\\\\\\`\\${" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1; ALSO `grep -c "out.*locale.*\\/index\\.html" e2e/phase7-onboarding-summary.spec.ts` returns 0 (no incorrect path).
    - File contains a comment documenting that `--test-match` CLI flag does NOT exist and `PW_TEST_MATCH` env var is the workaround: `grep -ci "PW_TEST_MATCH" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 2.
    - File contains a comment documenting that H1 grep on out/{locale}.html is INFORMATIONAL not blocker: `grep -ci "informational" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1.
    - File asserts FLAT-tile boundary: `grep -c "shadow-" e2e/phase7-onboarding-summary.spec.ts` returns ≥ 1 (the grep guard text — meta!).
    - `npx tsc --noEmit` exits 0 (the new spec compiles).
    - `npx eslint e2e/phase7-onboarding-summary.spec.ts` exits 0.
    - Vitest existing suite still passes: `npx vitest run` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npx eslint e2e/phase7-onboarding-summary.spec.ts && [ "$(grep -c 'test.describe(' e2e/phase7-onboarding-summary.spec.ts)" -ge "10" ] && [ "$(grep -c 'PW_TEST_MATCH' e2e/phase7-onboarding-summary.spec.ts)" -ge "2" ] && [ "$(grep -c '44px' e2e/phase7-onboarding-summary.spec.ts)" -ge "1" ] && wc -l e2e/phase7-onboarding-summary.spec.ts | awk '{ if ($1 >= 400) exit 0; else exit 1 }'</automated>
  </verify>

  <done>
    `e2e/phase7-onboarding-summary.spec.ts` created with 10+ describe blocks covering: onboarding 3-step desktop layout matrix (6 locales × 3 viewports), Enter-advance keyboard contract (EN + AR), summary page typography (6 × 3), export-button hover smoke test (EN at 1280px), focus-visible ring smoke test (EN at 1280px), SEO regression on `out/{locale}.html` (H1 count informational), aggregate physical-CSS grep guard (allowlist the pre-existing Calendar `left-3.5`), FLAT-tile boundary grep guard (Design DNA axis 4), i18n parity check (6-locale key-set equality), mobile invariant screenshot diff at 375px (EN + AR). Spec compiles + lints clean. Vitest baseline preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute the Phase 7 verification harness — build, serve, run the spec, run aggregate greps, run i18n parity, capture screenshots, prep human-verify package</name>

  <read_first>
    - The just-created e2e/phase7-onboarding-summary.spec.ts (verify it's present + the 10 describe blocks scaffold correctly)
    - playwright.config.ts (re-confirm the `PW_TEST_MATCH` hook reads the env var on line ~30)
    - package.json (verify `npm run build` exists; verify dependencies include `serve` or that `npx --yes serve` works)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-11-PLAN.md §"Task 2" or similar verification-harness task (pattern reference for the build+serve+run sequence)
  </read_first>

  <files>e2e/phase7-onboarding-summary.spec.ts</files>

  <action>
    Execute the verification harness in sequence:

    1. **Static build**: `npm run build` — exit 0 expected. If errors, fix in the appropriate Plan 07-01/02/03 and re-execute.

    2. **Serve the static export**: `npx --yes serve out -l 4173 --no-clipboard &` — runs in background. Document the PID for later kill (planner uses `&` so Bash returns immediately; the executor's task is to background+sleep+test+kill).

    3. **Run the Phase 7 spec**:
       `PHASE7_BASE_URL=http://localhost:4173 PW_TEST_MATCH='phase7-onboarding-summary\\.spec\\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line` — exit 0 expected (all 10 describe blocks pass).

    4. **Run the aggregate physical-CSS grep guard** (cross-checks the spec's internal assertion with a CLI-level check):
       `grep -nE "(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|left-|right-)[0-9]" src/components/onboarding/OnboardingFlow.tsx src/app/\\[locale\\]/summary/page.tsx src/components/export/ExportActions.tsx` — expected output: ONLY the pre-existing `OnboardingFlow.tsx:~220: <Calendar size={18} className="absolute left-3.5 top-1/2 ...` line. Any other physical CSS = Phase 7 regression.

    5. **Run the FLAT-tile boundary grep**:
       `grep -nE "shadow-(xl|lg|md|sm|xs)|ring-1 ring-black" src/app/\\[locale\\]/summary/page.tsx src/components/summary/*.tsx src/components/export/DaySummaryCard.tsx` — expected 0 matches.

    6. **Run the i18n parity check** (one-liner — count keys per locale via jq):
       `for l in en fr es pt zh ar; do echo "$l: $(jq 'paths(scalars) | join(".")' messages/$l.json | sort | wc -l) keys"; done` — all 6 locales should report identical key counts. If any differ, run `.claude/scripts/article-i18n-completeness.sh` (per CLAUDE.md §"Configuration") to surface the diff.

    7. **Capture human-verify screenshots**: the spec's Task-1 test groups already write screenshots to `test-results/phase7-onboarding-summary/`. Verify the directory contains:
       - 18 onboarding screenshots (6 locales × 3 viewports × Step 1) + 18 for Step 2 + 18 for Step 3 = 54
       - 18 summary screenshots (6 × 3)
       - 1 export-button hover screenshot (EN at 1280px)
       - 1 focus-visible ring screenshot (EN at 1280px)
       - 4 mobile invariant screenshots (EN + AR × onboarding-step1 + summary)
       - Total ~78 screenshots

    8. **Kill the local server**: `kill %1` or `pkill -f "serve out -l 4173"` (cleanup).

    9. **Print the human-verify summary** to stdout:
       ```
       === Phase 7 Verification Harness Output ===
       Build: PASS (npm run build exit 0)
       Spec: PASS (10/10 describe blocks; X tests passed)
       Aggregate physical-CSS guard: PASS (only pre-existing `absolute left-3.5` matched; allowlist confirmed)
       FLAT-tile boundary: PASS (0 shadows on in-page summary surfaces)
       i18n parity: PASS (6 locales × identical key counts)
       Screenshots: 78 written to test-results/phase7-onboarding-summary/
       Mobile diff: 2 back-pill +4px bumps confirmed in onboarding Step 2 + Step 3 (THE ONLY accepted Phase 7 mobile diff per UI-SPEC §"Hit-target audit")
       SEO regression: PASS (canonical/hreflang/lang/JSON-LD preserved across 6 out/{locale}.html files; H1 count informational)

       Ready for human-verify checkpoint.
       ```

    If any step fails, surface the exact error + the upstream plan (07-01 / 07-02 / 07-03) that owns the fix. Do NOT proceed to the human-verify checkpoint until all 9 steps PASS.
  </action>

  <acceptance_criteria>
    - `npm run build` exits 0 (no build regressions from Plans 07-01/02/03).
    - The Phase 7 spec passes: `PW_TEST_MATCH='phase7-onboarding-summary\.spec\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line` exits 0.
    - The aggregate physical-CSS grep returns ONLY the allowlisted `absolute left-3.5` line on OnboardingFlow.tsx (pre-existing Calendar icon position).
    - The FLAT-tile boundary grep returns 0 matches across summary surface files.
    - The i18n parity check shows 6 locales with identical key counts.
    - The `test-results/phase7-onboarding-summary/` directory exists and contains 70+ screenshot files (`.png`).
    - The local serve process is killed before the task completes (`pgrep -f "serve out -l 4173"` returns no matches).
    - The verification summary is printed to stdout in the format documented above.
  </acceptance_criteria>

  <verify>
    <automated>ls test-results/phase7-onboarding-summary/ | wc -l | awk '{ if ($1 >= 50) exit 0; else exit 1 }' && [ "$(grep -c 'shadow-' src/app/'[locale]'/summary/page.tsx src/components/summary/*.tsx src/components/export/DaySummaryCard.tsx 2>/dev/null | awk -F: '{ sum += $2 } END { print sum }')" = "0" ] && for l in en fr es pt zh ar; do jq 'paths(scalars) | join(".")' messages/$l.json | sort | wc -l; done | sort -u | wc -l | grep -q '^1$'</automated>
  </verify>

  <done>
    Verification harness fully executed: build clean, Phase 7 spec passes 10/10 describe blocks, aggregate physical-CSS guard PASSES with only the allowlisted pre-existing Calendar `left-3.5`, FLAT-tile boundary grep returns 0 shadows on summary surfaces, i18n parity confirms 6 locales at identical key counts, 70+ screenshots captured in test-results/phase7-onboarding-summary/, local serve killed, verification summary printed. The 2 back-pill +4px bumps on Onboarding Step 2 + Step 3 are confirmed as THE ONLY accepted Phase 7 mobile diff. Ready to proceed to the human-verify checkpoint in Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checkpoint — review screenshots + spec output + confirm Phase 7 desktop layout matches design intent</name>

  <what-built>
    Plans 07-01 + 07-02 + 07-03 + 07-04 deliver:
    - OnboardingFlow.tsx: 3 step h2 headlines bump `md:text-3xl`, age input scales `md:w-32 md:text-4xl`, unit toggles widen `md:max-w-[200px] md:py-8`, date input scales `md:py-4 md:text-lg`, 2 back-pill buttons bump to min-h-[44px] (THE only accepted Phase 7 mobile diff), Enter-advance keyboard contract on all 3 step containers, focus-visible rings on age input + date input + 2 unit toggles + 2 back pills.
    - summary/page.tsx: H1 bumps `md:text-4xl` + `md:px-0` (biggest Phase 7 typography bump), section H2s (storyTitle + lookBackTitle) bump `md:text-xl`, 3-stat top tiles bump padding `md:px-4 md:py-5`, Container stays `default` (no widening), FLAT-tile boundary preserved (no shadow / no ring on tiles — Design DNA axis 4 medical-rigor boundary).
    - ExportActions.tsx: PDF + CSV buttons gain `md:hover:-translate-y-px md:transition-all md:duration-150` (1px lift; Boomer-safe override 2 + 3 caps), outer wrap gains `md:max-w-2xl md:mx-auto` (capped + centered at md+), Button primitive variant hovers + focus-visible inherited unchanged.

    Phase 7 verification harness output (Task 2):
    - Build: PASS
    - Phase 7 spec: PASS (10/10 describe blocks)
    - Aggregate physical-CSS guard: PASS
    - FLAT-tile boundary: PASS
    - i18n parity: PASS
    - 70+ screenshots captured for review
    - 2 back-pill +4px bumps confirmed as the only accepted mobile diff

    Mobile (<768px) byte-equivalent across all 3 surfaces EXCEPT the 2 onboarding back pills which are +4px taller (accepted correctness fix per Boomer-safe override 1 — equivalent to Phase 5's NavLink fix).
  </what-built>

  <how-to-verify>
    1. **Review the Phase 7 verification spec output** from Task 2. All 10 describe blocks must report PASS in the Playwright reporter.

    2. **Review the screenshots** in `test-results/phase7-onboarding-summary/`:
       - **Onboarding Step 1** at 1280px in EN — verify the "Tell us your age" headline is visually larger (30px vs the 24px mobile baseline); the age input is 128px wide with a 36px-typed number; the input is centered, NOT looking like a tiny element swimming in whitespace.
       - **Onboarding Step 1** at 375px in EN — verify it looks identical to pre-Phase-7 (24px headline, 112px age input, 30px typed number). Zero visual diff.
       - **Onboarding Step 2** at 1280px in EN — verify the mL + oz toggles are each ~200px wide with ~32px vertical padding (visually generous). The back-pill button is +4px taller than pre-Phase-7 (accepted).
       - **Onboarding Step 2** at 375px in EN — verify it looks like pre-Phase-7 EXCEPT for the +4px taller back pill.
       - **Onboarding Step 3** at 1280px in EN — verify the date input has more vertical padding (~16px) and 18px text. The back-pill button is +4px taller.
       - **Summary page** at 1280px in EN — verify the H1 "Three days, captured." (or locale equivalent) is visually dominant at 36px. Section H2s render at 20px. 3-stat tiles render with more interior padding (~16px horizontal, 20px vertical) at md+. NO shadow on the tiles. NO `Container variant="wide"` widening (the page content stays inside ~768px max-w-3xl, not 1024px).
       - **Summary page** at 375px in EN — verify ZERO diff vs pre-Phase-7.
       - **Summary page** at 1280px in AR — verify the H1 + tiles render in RTL (the 3-stat grid right-to-left in source order: voidCount on the right). FLAT tiles (no shadow leaked in via RTL inheritance).
       - **Summary page** at 1280px in ZH — verify the H1 + section H2s render with proper CJK font fallback (no missing-glyph tofu boxes). The Inter font stack falls back to PingFang SC / Noto Sans CJK.
       - **Export buttons hover screenshot** (EN at 1280px) — verify the PDF Button is visually lifted by 1px (subtle but present) when hovered, and the bg-color has shifted to ipc-600 (a deeper amber). The CSV Button (when hovered) shifts to bg-ipc-100.
       - **Focus-visible ring screenshot** (EN at 1280px) — verify a visible ring-ipc-500 (amber) ring with 2px offset appears around the age input when focused via Tab.

    3. **Manual smoke test in a real browser** (open `http://localhost:4173/` after re-serving the build):
       - Tab through Step 1 — visible focus ring on age input
       - Type 50, press Enter — advances to Step 2
       - Press Enter — advances to Step 3
       - Press Enter — completes onboarding, navigates to /diary/day/1
       - Switch to a 1280px window — verify the same flow works with the desktop scaling
       - Switch to AR locale — verify the dir="rtl" doesn't break Enter-advance (Enter still goes forward)
       - Complete a diary (or seed via DevTools localStorage), navigate to /en/summary — hover the PDF button — verify the lift fires; click — verify the PDF generates

    4. **Confirm the accepted mobile diff is acceptable**:
       - The 2 back-pill buttons on Step 2 + Step 3 are 4px taller at all viewports (including 375px). This is the ONLY mobile diff in Phase 7.
       - Compare against Phase 5's NavLink precedent (which applied the same +4px correctness fix for Boomer-safe override 1's 44px hit-target floor).

    5. **Walkthrough_findings.md**: after Phase 7 ships and the daily walkthrough runs (separately, per CLAUDE.md memory `walkthrough_findings`), verify NO new findings are logged. (This is post-merge; for the human-verify checkpoint, we're confirming the verification spec covers what the daily walkthrough would catch.)
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 5 verification items above pass visual review.
    Type "issues: {description}" if any screenshot shows unexpected diffs, the manual smoke test fails, or the desktop layout doesn't feel intentional. Issues route back to the relevant Plan (07-01 / 07-02 / 07-03) for fix-up.
  </resume-signal>
</task>

</tasks>

<must_haves>
  truths:
    - "Running the Phase 7 spec (`PW_TEST_MATCH='phase7-onboarding-summary\\.spec\\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts`) exits 0"
    - "The 10 describe blocks in the spec cover: onboarding desktop layout matrix (6×3), Enter-advance keyboard (EN+AR), summary typography (6×3), export hover smoke test (EN×1280), focus-visible ring smoke test (EN×1280), SEO regression on out/{locale}.html, aggregate physical-CSS grep guard, FLAT-tile boundary grep guard, i18n parity check, mobile invariant screenshot diff at 375px"
    - "The H1 count assertion on `out/{locale}.html` is INFORMATIONAL (Phase 5 + Phase 6 lessons: H1 hydration race is a pre-existing concern; static HTML may have H1 count 0 — not a Phase 7 regression)"
    - "The aggregate physical-CSS grep across OnboardingFlow + summary + ExportActions returns ONLY the pre-existing `absolute left-3.5` on Calendar icon (allowlisted; UI-SPEC defers to Phase 8)"
    - "The FLAT-tile boundary grep returns 0 shadows on summary surfaces (Design DNA axis 4 medical-rigor boundary preserved)"
    - "The 6 locale message files have identical key counts (Phase 7 introduces ZERO new keys; regression guard on pre-existing parity)"
    - "70+ screenshots are captured in test-results/phase7-onboarding-summary/ for human-verify review"
    - "The local serve process is killed before the verification harness task completes"
    - "Human-verify checkpoint gates merge: user types 'approved' OR 'issues: {description}' routing back to the relevant Plan"
    - "The 2 back-pill +4px bumps (THE only accepted Phase 7 mobile diff) are confirmed via Playwright `await expect(backPill).toHaveCSS('min-height', '44px')` AND visual screenshot review"
  artifacts:
    - path: "e2e/phase7-onboarding-summary.spec.ts"
      provides: "10 describe blocks covering 6-locale × 3-viewport matrix for onboarding + summary + export-button verification; aggregate physical-CSS grep guard; FLAT-tile boundary grep guard; i18n parity check; mobile invariant screenshots; SEO regression on out/{locale}.html (H1 informational); Enter-advance keyboard contract in EN + AR; focus-visible ring smoke test"
      contains: ["from '@playwright/test'", "STORE_KEY", "LOCALES = ['en', 'fr', 'es', 'pt', 'zh', 'ar']", "PHASE7_BASE_URL", "PW_TEST_MATCH", "44px", "translateY", "test.describe(", "informational", "out/", "shadow-"]
      min_lines: 400
    - path: "test-results/phase7-onboarding-summary/"
      provides: "70+ screenshots covering onboarding 3 steps × 6 locales × 3 viewports + summary × 6 × 3 + export-button hover + focus-visible ring + mobile invariant diff"
      contains: ["*.png"]
  key_links:
    - from: "Phase 7 spec PHASE7_BASE_URL env var"
      to: "playwright.config.ts PW_TEST_MATCH hook on line ~30"
      via: "process.env env var lookup at config load time"
      pattern: "process.env.PW_TEST_MATCH"
    - from: "Phase 7 spec out/{locale}.html SEO check"
      to: "Next.js 16 + next-intl 'as-needed' static export pattern (locked by Phase 5's 05-07)"
      via: "fs.readFileSync(resolve('out', `${locale}.html`))"
      pattern: "out.*locale.*\\.html"
    - from: "Phase 7 spec STORE_KEY usage"
      to: "e2e/helpers/fixtures.ts (existing helper module)"
      via: "import { STORE_KEY } from './helpers/fixtures'"
      pattern: "from './helpers/fixtures'"
    - from: "human-verify checkpoint"
      to: "Plans 07-01 / 07-02 / 07-03 (routing back if issues surface)"
      via: "resume-signal 'issues: {description}' which the orchestrator parses"
      pattern: "issues: .*"
</must_haves>

<verification>
  - TypeScript: `npx tsc --noEmit` exits 0 (the new Playwright spec compiles under strict mode).
  - ESLint: `npx eslint e2e/phase7-onboarding-summary.spec.ts` exits 0.
  - Vitest existing suite still passes: `npx vitest run` exits 0 (no regressions from Phase 7 implementation OR verification harness).
  - Build: `npm run build` exits 0; `out/` directory regenerated.
  - The Phase 7 spec passes 10/10 describe blocks: `PW_TEST_MATCH='phase7-onboarding-summary\.spec\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line` exits 0.
  - Aggregate physical-CSS guard (CLI cross-check of the spec's internal assertion): `grep -nE "(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|left-|right-)[0-9]" src/components/onboarding/OnboardingFlow.tsx 'src/app/[locale]/summary/page.tsx' src/components/export/ExportActions.tsx | grep -v 'absolute left-3.5'` returns EMPTY output (only the allowlisted pre-existing Calendar `left-3.5` is excluded).
  - FLAT-tile boundary CLI guard: `grep -nE "shadow-(xl|lg|md|sm|xs)|ring-1 ring-black" 'src/app/[locale]/summary/page.tsx' src/components/summary/*.tsx src/components/export/DaySummaryCard.tsx` returns EMPTY output (0 matches).
  - i18n parity CLI check: `for l in en fr es pt zh ar; do jq 'paths(scalars) | join(".")' messages/$l.json | sort | wc -l; done | sort -u | wc -l` returns `1` (all 6 locales have identical key counts).
  - Screenshot count: `ls test-results/phase7-onboarding-summary/*.png | wc -l` returns ≥ 50.
  - Server cleanup: `pgrep -f "serve out -l 4173"` returns empty (no orphaned serve process).
  - Human-verify checkpoint: user has typed "approved" (or routed an issue back to 07-01/02/03).
  - All 5 ROADMAP Phase 7 success criteria verified:
    1. PASS — onboarding desktop layout (10 PW assertions across 6 locales × 3 viewports)
    2. PASS — summary "metric grid" delivered as 3-stat tile padding + FLAT boundary preserved (CONTEXT correction: the 5 IPC metrics live in CSV/PDF exports only; UI-SPEC §"Container variant decision" verified)
    3. PASS — export buttons hover (PW transform assertion + manual smoke test) + max-w-2xl wrap + focus-visible inherited
    4. PASS — RTL Arabic verified via 6-locale matrix + dir="rtl" check + aggregate physical-CSS guard
    5. PASS — mobile (<768px) byte-equivalent except the accepted 2 back-pill +4px bumps; daily walkthrough still green (post-merge)
</verification>

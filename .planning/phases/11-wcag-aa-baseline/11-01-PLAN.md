---
phase: 11-wcag-aa-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/diary/TimelineView.tsx
autonomous: true
requirements: [A11Y-01]
tags: [wcag, a11y, h1, semantic-heading, diary]

must_haves:
  truths:
    - "Every page in the app has exactly one <h1> after this plan ships"
    - "Diary day pages 1/2/3 render <h1> 'Day N' (day-view) or <h1> 'Night N' (night-view) — the heading the patient reads as their current location"
    - "The visible visual hierarchy is unchanged — the existing Tailwind classes (text-xl font-bold text-balance) and conditional night-mode color (text-indigo-100 / text-ipc-950) are preserved verbatim"
    - "axe-core wcag2a/wcag2aa rule 'page-has-heading-one' produces zero violations on /<locale>/diary/day/{1,2,3} for all 6 locales"
    - "No other page in the app gains or loses an <h1> as a side effect"
  artifacts:
    - path: "src/components/diary/TimelineView.tsx"
      provides: "Promoted <h1> at line 506 (replaces the existing <h2>) for the diary-day page header"
      contains: "<h1"
  key_links:
    - from: "src/components/diary/TimelineView.tsx (h1 element)"
      to: "axe-core rule 'page-has-heading-one'"
      via: "DOM has exactly one <h1>"
      pattern: "h1 sibling check passes when the page mounts"
    - from: "TimelineView.tsx h1 visual classes"
      to: "preserved Tailwind hierarchy"
      via: "className unchanged (text-xl font-bold + text-balance + night-mode color)"
      pattern: "no visual regression on diary day surface"
---

<objective>
Promote the current `<h2>` "Day N" / "Night N" heading inside `TimelineView.tsx` (line 506) to `<h1>`. This is the only h1 gap on a real, user-reachable surface in the app — audit-confirmed.

Purpose: A diary day page is the most-used surface in the app. WCAG 2.4.6 (Headings and Labels) + 1.3.1 (Info and Relationships) require a page-level heading; right now `DayPageClient` opens with an `<h2>` and screen readers parse the page as headingless. This is the single highest-impact a11y fix in Phase 11 by accessed-pageview share.

Output: A 1-character semantic change (`<h2>` → `<h1>`, `</h2>` → `</h1>`) on `src/components/diary/TimelineView.tsx:506`. The visible visual hierarchy is preserved exactly because the existing Tailwind classes (`text-xl font-bold text-balance` + the conditional `text-indigo-100` / `text-ipc-950` night-mode color) are unchanged.

**Why no other pages need touching in this plan:** I grepped every `src/app/[locale]/**/page.tsx` + every component that page routes delegate to:
- `src/app/[locale]/LandingContent.tsx` — already has `<h1>` (lines 119 + 185, conditional on diary-started state — only one ever renders)
- `src/app/[locale]/summary/page.tsx` — already has `<h1>` (lines 70 + 119, conditional on hydration/completion — only one ever renders)
- `src/app/[locale]/help/page.tsx:63` — already has `<h1>`
- `src/app/[locale]/privacy/page.tsx:46` — already has `<h1>`
- `src/app/[locale]/terms/page.tsx:61` — already has `<h1>`
- `src/app/[locale]/learn/page.tsx:111` — already has `<h1>`
- `src/app/[locale]/learn/[topic]/page.tsx:119/167` — already has `<h1>` (pillar / non-pillar branches; only one renders per page)
- `src/app/[locale]/learn/[topic]/[slug]/page.tsx:142` — already has `<h1>`
- `src/app/[locale]/learn/[topic]/page/[page]/page.tsx:111` — already has `<h1>`
- `src/app/[locale]/learn/glossary/page.tsx:76` — already has `<h1>`
- `src/app/[locale]/learn/glossary/[term]/page.tsx:91` — already has `<h1>`
- `src/app/[locale]/learn/for-men/page.tsx:79` — already has `<h1>`
- `src/app/[locale]/learn/for-women/page.tsx:79` — already has `<h1>`
- `src/app/[locale]/learn/authors/[slug]/page.tsx:109` — already has `<h1>`
- `src/app/[locale]/learn/articles/page.tsx` — delegates to `ArchiveContent.tsx:50` — has `<h1>`
- `src/app/[locale]/learn/articles/page/[page]/page.tsx` — same `ArchiveContent.tsx` — has `<h1>`
- `src/app/[locale]/diary/page.tsx` — redirect-only, never renders UI (no h1 needed)
- `src/app/page.tsx` — redirect-only, never renders UI (no h1 needed)
- `src/components/diary/Day1Celebration.tsx:97` — already has `<h1>` (it is a full-screen `role="dialog"` overlay that fully covers the page; while it is mounted, IT is the page heading, and it correctly uses `aria-labelledby="day1-celebration-title"` on the dialog)
- `src/app/[locale]/diary/day/[dayNumber]/page.tsx` → `DayPageClient.tsx` → `TimelineView.tsx:506` — THIS is the only gap, and this plan fixes it

So the goal-backward derivation says: ship one targeted change on one line, preserve every existing class, verify via `grep` + axe-core that the diary surface is the only page that changed and that the count of `<h1>` per rendered page is exactly 1 across all 18 routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-wcag-aa-baseline/11-CONTEXT.md
@.planning/audits/2026-05-18-comprehensive-audit/UI-REVIEW.md
@.planning/codebase/CONVENTIONS.md
@docs/UX_PHILOSOPHY.md
@src/components/diary/TimelineView.tsx

<interfaces>
<!-- The exact existing element to promote. Lines 505-511 of TimelineView.tsx. -->
<!-- Both the day-view header and the night-view header use the same h2 element via the isNighttime branch. -->

Current (TimelineView.tsx:505-511):
```tsx
<div className="text-center">
  <h2 className={`text-xl font-bold text-balance ${isNighttime ? 'text-indigo-100' : 'text-ipc-950'}`}>
    {isNighttime ? tc('night', { number: dayNumber - 1 }) : tc('day', { number: dayNumber })}
  </h2>
  <span className={`font-medium text-sm ${isNighttime ? 'text-indigo-300/80' : 'text-ipc-700'}`}>
    {dayLabel}
  </span>
```

After this plan (TimelineView.tsx:505-511):
```tsx
<div className="text-center">
  <h1 className={`text-xl font-bold text-balance ${isNighttime ? 'text-indigo-100' : 'text-ipc-950'}`}>
    {isNighttime ? tc('night', { number: dayNumber - 1 }) : tc('day', { number: dayNumber })}
  </h1>
  <span className={`font-medium text-sm ${isNighttime ? 'text-indigo-300/80' : 'text-ipc-700'}`}>
    {dayLabel}
  </span>
```

That is the only change. No other elements in the file are touched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the TimelineView day/night heading from h2 to h1</name>
  <files>src/components/diary/TimelineView.tsx</files>
  <action>
    Open `src/components/diary/TimelineView.tsx`. Locate the day-header heading inside the `<div className="text-center">` block (currently line ~505-508). The element renders the page-level "Day N" or "Night N" title for the diary day page.

    Change exactly two tokens on the element:
    - `<h2 className=` → `<h1 className=`
    - the matching closing `</h2>` → `</h1>`

    Do NOT modify the `className` value. Do NOT touch the surrounding `<div className="text-center">`, the `<span>` underneath (which carries `dayLabel`), or the `text-xs` `progressSubtitle` block.

    Do NOT add any new aria attribute (the heading already carries its accessible name via its text content; the surrounding `aria-label`s on the prev/next links are unrelated and stay intact).

    Do NOT introduce any other change in this file. The `<h2>` → `<h1>` swap is the entire deliverable.

    Rationale (so a future maintainer reading the diff understands): the audit (UI-REVIEW.md §A1, finding I3 in 11-CONTEXT.md) flagged that DayPageClient has no `<h1>` anywhere on the page; the visible "Day 1" header was rendered at h2 with no enclosing h1. Promoting that very element to h1 is the smallest possible fix that makes the diary day surface comply with WCAG 2.4.6 + 1.3.1 without introducing a second heading, without disturbing the visual hierarchy, and without adding an sr-only-only h1 that screen-reader users would hear differently from sighted users (we want the heading users SEE to also be the page's accessible h1 — single source of truth).

    Per CONVENTIONS.md (this repo's style): `<h2>` → `<h1>` is the entire change. No comments need to be added — the audit + the plan capture the rationale. Inline code comments are reserved for non-obvious decisions per the conventions doc; this change IS the obvious one.
  </action>
  <verify>
    <automated>
    # Exactly one h1 in TimelineView.tsx, exactly zero remaining at the old day-header position (which carried the night-mode color tokens).
    test "$(grep -c '<h1 className=' src/components/diary/TimelineView.tsx)" = "1" \
      && test "$(grep -c "isNighttime ? 'text-indigo-100' : 'text-ipc-950'" src/components/diary/TimelineView.tsx)" = "1" \
      && grep -q "<h1 className=\`text-xl font-bold text-balance .*text-indigo-100.*text-ipc-950" src/components/diary/TimelineView.tsx \
      && ! grep -q "<h2 className=\`text-xl font-bold text-balance .*text-indigo-100.*text-ipc-950" src/components/diary/TimelineView.tsx \
      && npx tsc --noEmit \
      && npx eslint src/components/diary/TimelineView.tsx
    </automated>
  </verify>
  <done>
    The day-header element in TimelineView.tsx uses `<h1>` instead of `<h2>`. The day-view (text-ipc-950) and night-view (text-indigo-100) conditional class tokens are preserved on the same line as before the change. `tsc --noEmit` and `eslint` both pass on the modified file. No other heading in the file changed (the rest of the file's `<h2>`/`<h3>` references stay as they are — they are subordinate section headings, correctly nested under the new page-level h1).
  </done>
</task>

<task type="auto">
  <name>Task 2: Aggregate audit — exactly one h1 per rendered route, no double-h1, no missing-h1</name>
  <files>(read-only — no source modifications)</files>
  <action>
    This is a guard task. It does NOT modify any source file. It verifies via static analysis that the project still has the expected "exactly one h1 per rendered page" invariant after Task 1 ships, and that no page mounts two `<h1>` elements via a branch we missed.

    The h1 audit done at planning time (see `<objective>`) identified these h1 sources:

    Page surface → h1 source file → element line:
    1. `/` (root) → `src/app/page.tsx` → redirect-only, no UI rendered, no h1 expected
    2. `/<locale>` (landing) → `src/app/[locale]/LandingContent.tsx` lines 119 + 185 (conditional branches — only one ever mounts at a time)
    3. `/<locale>/diary/day/{1,2,3}` (diary) → `src/components/diary/TimelineView.tsx:506` (NEW — promoted by Task 1)
    4. `/<locale>/diary` (diary index) → `src/app/[locale]/diary/page.tsx` redirect-only, no UI
    5. `/<locale>/summary` (summary) → `src/app/[locale]/summary/page.tsx` lines 70 + 119 (conditional — hydration vs ready)
    6. `/<locale>/help` → `src/app/[locale]/help/page.tsx:63`
    7. `/<locale>/privacy` → `src/app/[locale]/privacy/page.tsx:46`
    8. `/<locale>/terms` → `src/app/[locale]/terms/page.tsx:61`
    9. `/<locale>/learn` → `src/app/[locale]/learn/page.tsx:111`
    10. `/<locale>/learn/<topic>` → `src/app/[locale]/learn/[topic]/page.tsx:119` (pillar) OR `:167` (non-pillar) — conditional, exactly one renders
    11. `/<locale>/learn/<topic>/<slug>` → `src/app/[locale]/learn/[topic]/[slug]/page.tsx:142`
    12. `/<locale>/learn/<topic>/page/<n>` → `src/app/[locale]/learn/[topic]/page/[page]/page.tsx:111`
    13. `/<locale>/learn/glossary` → `src/app/[locale]/learn/glossary/page.tsx:76`
    14. `/<locale>/learn/glossary/<term>` → `src/app/[locale]/learn/glossary/[term]/page.tsx:91`
    15. `/<locale>/learn/articles` → delegates to `src/components/learn/ArchiveContent.tsx:50`
    16. `/<locale>/learn/articles/page/<n>` → same `ArchiveContent.tsx`
    17. `/<locale>/learn/for-men` → `src/app/[locale]/learn/for-men/page.tsx:79`
    18. `/<locale>/learn/for-women` → `src/app/[locale]/learn/for-women/page.tsx:79`
    19. `/<locale>/learn/authors/<slug>` → `src/app/[locale]/learn/authors/[slug]/page.tsx:109`

    Plus the Day1Celebration overlay (`src/components/diary/Day1Celebration.tsx:97`) — this is a `role="dialog"` full-screen overlay layered above DayPageClient. When it's mounted, the day page's h1 still exists in the DOM under the dialog, which is acceptable: axe-core's `page-has-heading-one` rule cares about presence, not visual layering; the dialog is `aria-modal="true"` with `aria-labelledby="day1-celebration-title"` so its own title is correctly announced as the modal label. This is NOT a double-h1 violation — it's a modal-on-page pattern.

    Run these greps from the repo root to confirm no source file gained or lost h1 elements unexpectedly. Each command emits a count; the expected counts are listed.

    ```
    # Total h1 element opening tags across src/. After Tasks 1 + 3 each promote one h2→h1 (TimelineView + OnboardingFlow), the count goes UP by exactly 2.
    # Before: 19 h1 opening tags (verified by plan-check re-grep against current tree). After: 21.
    grep -rn "<h1\b" src/app src/components | wc -l
    # Expected: 21

    # The new h1 in TimelineView.tsx (exactly one).
    grep -c "<h1 className=" src/components/diary/TimelineView.tsx
    # Expected: 1

    # Day1Celebration's h1 still has its aria-labelledby anchor unchanged.
    grep -q 'id="day1-celebration-title"' src/components/diary/Day1Celebration.tsx \
      && grep -q 'aria-labelledby="day1-celebration-title"' src/components/diary/Day1Celebration.tsx \
      && echo OK
    # Expected: "OK"
    ```

    Document the counts in the plan summary (Plan SUMMARY .md, when written). If a grep returns a count that does NOT match expectations, the executor must STOP and surface what changed — that means a different h1 source moved or was added/removed without anyone noticing, which would change the audit assumptions.

    Per CLAUDE.md: this plan is run inside the GSD workflow; it does not bypass any enforcement. The grep counts are recorded in the summary as evidence the audit invariants still hold.
  </action>
  <verify>
    <automated>
    set -e
    # 21 h1 openings across src/app + src/components after Tasks 1 + 3 ran (was 19 before promotion).
    count=$(grep -rn '<h1\b' src/app src/components | wc -l | tr -d ' ')
    test "$count" = "21" || { echo "Expected 21 h1 openings, got $count"; exit 1; }
    # TimelineView gained exactly one h1.
    test "$(grep -c '<h1 className=' src/components/diary/TimelineView.tsx)" = "1"
    # OnboardingFlow gained exactly one h1 (Task 3 promotion — closes the `/<locale>` showOnboarding-state h1 gap).
    test "$(grep -c '<h1 className=' src/components/onboarding/OnboardingFlow.tsx)" = "1"
    # Day1Celebration dialog ID + aria-labelledby still wired.
    grep -q 'id="day1-celebration-title"' src/components/diary/Day1Celebration.tsx
    grep -q 'aria-labelledby="day1-celebration-title"' src/components/diary/Day1Celebration.tsx
    </automated>
  </verify>
  <done>
    A read-only grep audit confirms: (a) the source tree now contains 21 `<h1>` opening tags (was 19 pre-Tasks-1+3), (b) exactly one of the new openings is in TimelineView.tsx and one in OnboardingFlow.tsx, (c) Day1Celebration.tsx still wires its own `aria-labelledby="day1-celebration-title"` to its h1, so its modal accessibility is intact. No other file has been mutated by this plan. If any expected count is wrong, the failure surfaces the unexpected change for the executor to investigate before commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Promote `OnboardingFlow.tsx` active-step `<h2>` to `<h1>` (closes the `/<locale>` showOnboarding-state h1 gap)</name>

  <read_first>
    - src/components/onboarding/OnboardingFlow.tsx (lines 107, 149, 211 — three `<h2>` step headings; only one ever mounts at a time per active wizard step)
    - src/app/[locale]/LandingContent.tsx (line 175-177 — the `showOnboarding` branch that delegates to `<OnboardingFlow>`; line 119 and 185 — the other branches' existing `<h1>` headings)
  </read_first>

  <files>src/components/onboarding/OnboardingFlow.tsx</files>

  <action>
    Plan-check finding (B2): `LandingContent.tsx` has 4 render branches. Branches 2 + 4 each render an `<h1>`. Branch 3 (`showOnboarding`) delegates to `<OnboardingFlow>` which uses only `<h2>` step headings, producing 0 h1 on `/{locale}` during the onboarding state. WCAG 2.4.6 + 1.3.1 fail on the landing route in this state.

    Fix: promote the active-step heading in each `OnboardingFlow` step from `<h2>` to `<h1>`. Only one of the 3 step components renders at a time (wizard pattern), so exactly one h1 mounts during onboarding — preserving the "exactly one h1 per page" invariant.

    Surgical edit at each of the 3 sites (lines 107, 149, 211 — verify line numbers by grep before editing):
    ```
    <h2 className="..."> → <h1 className="...">
    </h2>                → </h1>
    ```

    DO NOT alter the Tailwind classes — only the tag name. Step indicator + visual hierarchy stays identical.

    Constraints per CLAUDE.md + memory:
    - No visual regression (semantic-only change; Tailwind classes preserved)
    - No new i18n keys (the step headings already use translated text)
    - TS strict + ESLint clean
    - NO `as any`, NO `@ts-ignore`
  </action>

  <verify>
    <automated>
    set -e
    # OnboardingFlow gained exactly 3 h1 openings (one per step component, only one mounts at a time).
    test "$(grep -c '<h1\b' src/components/onboarding/OnboardingFlow.tsx)" = "3"
    # Same count of h1 closings.
    test "$(grep -c '</h1>' src/components/onboarding/OnboardingFlow.tsx)" = "3"
    # No h2 left where the h1s used to be (the 3 step headings were converted).
    npx tsc --noEmit
    npx eslint src/components/onboarding/OnboardingFlow.tsx
    </automated>
  </verify>

  <done>
    `OnboardingFlow.tsx` renders an `<h1>` (not `<h2>`) for the active step heading in all 3 wizard steps. The `/{locale}` showOnboarding-state h1 gap (plan-check B2) is closed. Branch 3 of LandingContent now has exactly one h1 in the DOM (the active OnboardingFlow step), matching branches 2 and 4. TS + ESLint clean.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, from the repo root:

```
# Static guards
test "$(grep -rn '<h1\b' src/app src/components | wc -l | tr -d ' ')" = "18"
test "$(grep -c '<h1 className=' src/components/diary/TimelineView.tsx)" = "1"
! grep -n "<h2 className=\`text-xl font-bold text-balance .*text-indigo-100.*text-ipc-950" src/components/diary/TimelineView.tsx
grep -q 'id="day1-celebration-title"' src/components/diary/Day1Celebration.tsx
grep -q 'aria-labelledby="day1-celebration-title"' src/components/diary/Day1Celebration.tsx

# Type-check + lint
npx tsc --noEmit
npx eslint src/components/diary/TimelineView.tsx

# Existing test suite stays green (no test depended on the h2 selector — verified via grep)
test -z "$(grep -rn '<h2.*text-xl' src/__tests__ e2e 2>/dev/null)"  # no selector relied on h2-only
```

All MUST exit 0.

The axe-core `page-has-heading-one` rule is verified end-to-end by Plan 11-04 (verification spec, Wave 2). That spec deep-links to `/<locale>/diary/day/1` in every locale and confirms exactly one h1 exists; if Task 1 in this plan fails, that spec catches the regression in Wave 2.
</verification>

<success_criteria>
- TimelineView.tsx line 506 renders `<h1>` instead of `<h2>`, preserving every existing class token verbatim.
- Total `<h1>` opening-tag count across `src/app` + `src/components` is exactly 18 (was 17 before this plan).
- `tsc --noEmit` and `eslint` pass on the modified file.
- No other file in the repo was modified by this plan.
- The day-view and night-view branches of the conditional className are unchanged (verified by the regex grep guarding both branches still appear on the same line).
</success_criteria>

<output>
After completion, create `.planning/phases/11-wcag-aa-baseline/11-01-SUMMARY.md` summarizing: the one-line semantic change in TimelineView.tsx, the h1 count delta (17 → 18), confirmation that no other file changed, and a note that downstream Plan 11-04 supplies the axe-core end-to-end proof.
</output>
</content>

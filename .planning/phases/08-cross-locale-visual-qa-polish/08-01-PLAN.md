---
plan_id: "08-01"
phase: 8
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/summary/page.tsx
  - src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx
  - src/components/diary/QuickLogFAB.tsx
  - src/components/layout/PrivacyNotice.tsx
  - src/components/layout/AppShell.tsx
  - .claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md
requirements_addressed: ["DTUX-06"]
autonomous: false
must_haves:
  truths:
    - "C1 closed: `out/{locale}/summary.html` for all 6 locales contains a non-empty `<h1>` in pre-hydration static HTML (SEO grep returns ≥ 1 per locale). Note: the summary page is nested under the [locale] segment, so the path is `out/{locale}/summary.html`, NOT `out/{locale}.html` (which is the landing page)."
    - "C2 closed: PrivacyNotice does NOT overlap the QuickLogFAB or any bottom-of-page content at 375px, 768px, 1280px in any of the 6 locales — verified visually + via screenshot at 4 viewport-locale combinations"
    - "C3 closed: deep-link to `/diary/day/2` (or `/diary/day/3`) before hydration shows a stable loading skeleton, then resolves to EITHER the day view (if the store has prior-day state) OR a single redirect to onboarding (if no state) — no flicker, no double-render, no race"
    - "C4 closed: the 3 QuickLogFAB speed-dial chips render with `min-h-[44px]` (Boomer-safe override 1) verified via computed CSS in a real browser at 375px AND 1280px"
    - "visual-qa skill matrix (scoped: 3 priority routes × 6 locales × 3 widths = 54 screens + 5 secondary routes × {en, ar} × 1280px = 10 screens = 64 screens total) produces ZERO NEW findings (cutoff: 2026-05-14 = Phase 5 start)"
    - "Daily 6-locale walkthrough (`e2e/walkthrough.spec.ts`) still passes — gate is `walkthrough_findings.md` gains no new entries from a post-Phase-8 run"
    - "ZERO new i18n keys introduced (Phase 8 is className + CSS edits, not copy)"
    - "ZERO physical CSS (`ml-`/`mr-`/`pl-`/`pr-`/`left-`/`right-`/`text-left`/`text-right`/`border-l-`/`border-r-`) introduced by this plan; pre-existing physical CSS (Calendar icon `absolute left-3.5`, DaySummaryCard `ml-2`/`ml-0.5`, LandingContent PlayCircle `mr-2`, BottomNav/Toast `left-0 right-0` full-width chrome) is logged + verified non-breaking in RTL, not blindly migrated"
  artifacts:
    - path: "src/app/[locale]/summary/page.tsx"
      provides: "Summary page with C1 fix — `<h1>` rendered outside the `useStoreHydrated()` gate so it appears in pre-hydration static HTML"
      contains: ["heroTitle"]
    - path: "src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx"
      provides: "DayPageClient with C3 fix — redirect logic gated on `useStoreHydrated()` matching the LandingContent + summary pattern"
      contains: ["useStoreHydrated", "hydrated"]
    - path: "src/components/diary/QuickLogFAB.tsx"
      provides: "QuickLogFAB with C4 fix — 3 speed-dial chips bumped to `min-h-[44px]` (Boomer-safe override 1)"
      contains: ["min-h-[44px]"]
    - path: "src/components/layout/PrivacyNotice.tsx"
      provides: "PrivacyNotice with C2 fix — re-positioned so it does not overlap the QuickLogFAB or bottom content at any viewport"
    - path: ".claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md"
      provides: "Updated findings log — pre-Phase-5 historical entries logged as `[deferred-pre-phase-5]`; any NEW findings surfaced by Phase 8 audit logged with `[2026-05-17]` timestamp; the 4 closed carry-overs (C1/C2/C3/C4) noted as `closed by Phase 8`"
  key_links:
    - from: "src/app/[locale]/summary/page.tsx `<h1>` element"
      to: "static export `out/{locale}/summary.html` SEO surface"
      via: "Next.js 16 RSC server render path (H1 placed OUTSIDE the `if (!hydrated) return <spinner/>` gate)"
      pattern: "heroTitle"
    - from: "src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx redirect useEffects (lines 129–140)"
      to: "useStoreHydrated() hook from @/lib/store"
      via: "gate-condition `if (!hydrated) return` before any redirect; render stable loading skeleton until hydrated"
      pattern: "useStoreHydrated"
    - from: "src/components/diary/QuickLogFAB.tsx speed-dial chip buttons (lines 67, 80, 93)"
      to: "Boomer-safe override 1 (44px hit-target floor)"
      via: "`min-h-[44px]` className addition on each of the 3 chip `<button>` elements"
      pattern: "min-h-\\[44px\\]"
    - from: "src/components/layout/PrivacyNotice.tsx `bottom-20` positioning (line 38)"
      to: "src/components/diary/QuickLogFAB.tsx `bottom-24 md:bottom-8` positioning (line 59)"
      via: "vertical-stacking math — PrivacyNotice bottom-20 (80px) sits between FAB bottom-24 (96px) and BottomNav (0–60px); the audit verifies they don't visually overlap at any viewport"
      pattern: "bottom-(20|24)"
---

# Plan 08-01: Bundled — visual-qa scoped-down matrix (64 screens) + 4 carry-overs (C1/C2/C3/C4) closed inline with named commits

<objective>
This is the bundled Phase 8 plan per the user gate locked in 08-CONTEXT.md §"Decisions" → "Plan structure". It executes Bucket A (visual-qa skill matrix run) AND Bucket B (4 carry-over closures) in ONE plan with atomic per-task commits. There is no separate audit-then-fix split.

**Phase 8 goal (from ROADMAP §"Phase 8" + 08-CONTEXT.md):** the full 6-locale × LTR/RTL × md/lg/xl matrix passes visual QA against the production-rendered site; AND the 4 specific carry-overs surfaced in Phases 5/6/7 verification are closed with named commits.

**Goal-backward analysis — for each of the 6 ROADMAP success criteria + the 4 carry-over closures, the plan contains the task that delivers it:**

| Success criterion (ROADMAP §"Phase 8") | Task that delivers it |
|---|---|
| 1. visual-qa matrix produces ZERO new findings | Task 1 (audit) + Task 6 (final walkthrough verification gate) |
| 2. NO `ml-`/`mr-`/`pl-`/`pr-`/`left-`/`right-` physical CSS introduced by Phases 5–7 remains | Task 1 (audit step C: physical-CSS scan with pre-Phase-5 cutoff) |
| 3. ZH + AR proper font fallback; PT + AR long-translation overflow fits | Task 1 (audit step B: font-fallback + overflow scan in 6-locale walk) |
| 4. Focus rings visible on white / `bg-ipc-50` / dark hover; AA contrast ≥ 4.5:1 on focus + hover | Task 1 (audit step D: focus + hover contrast scan) |
| 5. Daily 6-locale walkthrough still passes | Task 6 (final walkthrough gate — confirms `e2e/walkthrough.spec.ts` exit 0 + no new entries in `walkthrough_findings.md`) |
| 6a. C1 closed (H1 hydration on summary) | Task 2 (named commit: `fix(08): C1 — render summary H1 outside hydration gate for SEO`) |
| 6b. C2 closed (PrivacyNotice overlap) | Task 3 (named commit: `fix(08): C2 — PrivacyNotice no longer overlaps FAB or bottom content`) |
| 6c. C3 closed (DayPageClient redirect race) | Task 4 (named commit: `fix(08): C3 — gate DayPageClient redirects on store hydration`) |
| 6d. C4 closed (QuickLogFAB speed-dial 44px) | Task 5 (named commit: `fix(08): C4 — QuickLogFAB speed-dial min-h-[44px] (Boomer-safe override 1)`) |

**visual-qa scope decision (planner pick, per CONTEXT §"Acceptable scope-reductions"):** the **scoped-down 64-screen matrix** is chosen over the full 144-screen matrix. Rationale:
- User has signaled limit-consciousness multiple times this session (CONTEXT records this explicitly).
- The 3 priority routes (`/`, `/diary/day/1`, `/summary`) carry ~90% of the Phase 5/6/7 regression risk because they are where the chrome + forms + summary work landed.
- The 5 secondary routes (learn, learn-article, help, privacy, terms) were NOT modified by Phases 5/6/7 except for chrome adoption (Header/Footer/Container), so a smoke pass at en+ar × 1280px is sufficient.
- The walkthrough_findings.md file already logs pre-Phase-5 a11y findings for /learn surfaces that are explicitly Phase 8 OUT-of-scope; auditing them in full would surface noise.

**Scope breakdown (64 screens total):**
- 3 priority routes × 6 locales × 3 widths (375/768/1280) = **54 screens**
- 5 secondary routes × 2 locales (en + ar for LTR/RTL parity) × 1 width (1280px) = **10 screens**
- Carry-over verification: 4 carry-overs × focused screenshot capture (not part of the 64; in-task verification)

**Cutoff for "new" findings (per CONTEXT §"Done criterion"):** `git log --since "2026-05-14"` is the boundary. Pre-Phase-5 findings (a11y entries from 2026-05-09, the long-standing color-contrast on summary, etc.) log to `walkthrough_findings.md` with `[deferred-pre-phase-5]` tag but DO NOT block Phase 8. NEW findings (introduced by Phases 5/6/7 OR surfaced for the first time by Phase 8 audit) MUST close before Phase 8 ships.

**Tool preference order (per CONTEXT §"Execution context" + skill §"Tools used"):**
1. Chrome MCP (`mcp__Claude_in_Chrome__*`) — real browser, mandatory for RTL/font-fallback failure modes (static HTML grep won't catch tofu boxes)
2. Preview MCP (`mcp__Claude_Preview__preview_*`) — Next dev server tooling
3. Bash + curl — fallback if both MCPs unavailable

**Server target:** prefer `npx next dev --turbo` on port 3050 (skill default) for live re-render after fixes. Production `myflowcheck.com` is the gold standard for the FINAL pass after fixes ship.

**This plan is `autonomous: false`** because Task 1 contains a human-verify checkpoint after the audit + 4 fixes complete but before the final walkthrough gate. The checkpoint reviews the screenshot package + the 4 carry-over fixes + confirms intent.

**Out of scope for this plan (per CONTEXT §"Deferred Ideas"):**
- Stabilization Phase 3 (STAB-06/07/08): toast dedup, export-error toast, clinicCode validation
- New i18n keys: visual-qa fixes are className + CSS edits; copy issues surface to `i18n-sync` skill via the deferred list, NOT bundled
- New features: Phase 8 is correctness + polish only
- Article content quality: flows through `naturalize-prose` + `i18n-sync`, not Phase 8
- Pre-Phase-5 historical findings in `walkthrough_findings.md` (logged with `[deferred-pre-phase-5]` tag, not closed)
- PWA install / service-worker behavior: separate concern (Stabilization)
- Performance audit (LCP / CLS / INP): distinct from visual-QA; future quick-task
- Pre-existing `absolute left-3.5` on Calendar icon in OnboardingFlow.tsx (UI-SPEC §"Step 3" defers); verify it doesn't break RTL, don't blindly migrate
- Pre-existing `ml-2` / `ml-0.5` in `src/components/export/DaySummaryCard.tsx` (lines 41, 57, 71); verify RTL parity, don't blindly migrate unless audit shows visual break

**Constraints from CLAUDE.md + memory + CONTEXT.md:**
- All 6 locales remain at parity (Stop hook + pre-commit hook enforce article translation coverage)
- Mobile-pristine HARD CONSTRAINT (CONTEXT 05 §"Mobile invariants" First Principle, locked 3× by user)
- Boomer-safe overrides (44px hit-target minimum, ≤200ms animations, modal close 3 paths) — C4 is one such fix
- Logical CSS only (`ms-`/`me-`/`ps-`/`pe-`/`start-`/`end-`); any new physical CSS = regression
- No em-dashes in UI strings; collaborative tone, not clinical
- ZH/AR fonts fall back from Inter to system stack (PingFang SC / Noto Sans CJK / Noto Naskh Arabic); audit visual weight
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Execute visual-qa skill audit on the scoped-down 64-screen matrix; log findings to walkthrough_findings.md; categorize NEW (must-fix this plan) vs DEFERRED (pre-Phase-5 historical, tagged and not closed)</name>

  <read_first>
    - .claude/skills/visual-qa/SKILL.md (FULL — methodology, route matrix, failure mode taxonomy, tool preference order)
    - .planning/phases/08-cross-locale-visual-qa-polish/08-CONTEXT.md §"Specific Ideas" → "visual-qa skill route matrix coverage" + "Phase 5/6/7 file delta = where the regression risk lives" + "Acceptable scope-reductions" (the scoped 64-screen breakdown documented above)
    - .planning/phases/05-layout-foundation-appshell-chrome/05-CONTEXT.md §"Mobile invariants" First Principle + §"Boomer-safe overrides" + §"Design DNA — Aesthetic axes" 8-axis table
    - .planning/phases/06-diary-forms-keyboard-navigation/06-CONTEXT.md (BottomSheet desktop-modal pattern — Phase 8 verifies the 5 forms render correctly across locales)
    - .planning/phases/07-onboarding-summary-surfaces/07-CONTEXT.md + 07-UI-SPEC.md (typography + tile padding decisions to verify hold cross-locale)
    - .claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md (FULL — to know what pre-existing findings to tag as `[deferred-pre-phase-5]` vs which to close)
    - playwright.config.ts (line ~30 for `PW_TEST_MATCH` env-var pattern if any audit needs to spawn a Playwright run)
  </read_first>

  <files>.claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md</files>

  <action>
    Execute the visual-qa skill audit per `.claude/skills/visual-qa/SKILL.md` workflow Steps 1–7, against the SCOPED-DOWN 64-screen matrix. The skill defines its own methodology — DO NOT redesign it. Follow it verbatim.

    **Step 0 — Boot dev server (skill Step 1):**
    ```bash
    pkill -f "next dev" 2>/dev/null; sleep 1
    rm -f .next/dev/lock
    nohup env PORT=3050 npx next dev --turbo > /tmp/visual-qa-phase8.log 2>&1 & disown
    sleep 14
    curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3050/en
    ```
    Verify 200 before proceeding. If 500+, check `/tmp/visual-qa-phase8.log` for build errors and surface to user.

    **Step A — Define the matrix (64 screens):**

    Priority routes (3) × locales (6) × widths (3) = 54 screens:
    - `/<locale>` (LandingContent.tsx — onboarding entry, PWA prompt, diary-in-progress CTA)
    - `/<locale>/diary/day/1` (DayPageClient — timeline, FAB, NextStepBanner)
    - `/<locale>/summary` (summary/page.tsx — H1, 3-stat tiles, story timeline, exports)
    - Locales: en, fr, es, pt, zh, ar
    - Widths: 375px (mobile), 768px (md), 1280px (lg/xl)

    Secondary routes (5) × {en, ar} × 1280px = 10 screens:
    - `/<locale>/learn`
    - `/<locale>/learn/post-prostatectomy/pee-a-lot-after-surgery` (any populated article works; pick one that exists in all 6 locales)
    - `/<locale>/help`
    - `/<locale>/privacy`
    - `/<locale>/terms`
    - Locales: en, ar
    - Widths: 1280px

    **Step B — Walk the matrix per skill §"Step 3 — Walk the matrix" + §"Step 4 — Triage issues by failure mode":**

    For each (route, locale, width) in the matrix, capture:
    1. Full-page screenshot via Chrome MCP `mcp__Claude_in_Chrome__*` (preferred), falling back to Preview MCP if Chrome unavailable. Retry once after 10s if MCP says "not connected" before falling back.
    2. Read accessibility tree (`read_page` / `preview_snapshot`) — confirm heading hierarchy, landmark roles, visible text.
    3. Scan for the 7 failure modes per skill §"Step 4":
       - **A. RTL physical-CSS leaks** (ar only) — text-align physical, ml/mr/pl/pr instead of ms/me/ps/pe, inline `style={{ left: ... }}`, icons not mirroring chevrons/arrows, `translateX(-50%)` not flipping
       - **B. Text overflow / wrap break** (pt, ar, zh) — long PT translations breaking button widths, chip rails overflowing, article titles overflowing `text-balance`
       - **C. Font fallback for non-Latin scripts** (zh, ar) — Inter doesn't ship CJK/Arabic glyphs, verify system fallback (PingFang SC / Noto Sans CJK / Noto Naskh Arabic) renders with acceptable weight; numerals confirm Western Arabic 0-9 in ar
       - **D. Contrast & accessibility** — IPC palette text colors on bg-surface ≥ 4.5:1 AA; button hover/active/disabled clear AA; focus rings visible at all sizes (≥ 2px outset, ipc-500 amber)
       - **E. Spacing / rhythm consistency** — py-/mb-/gap- values follow the IPC scale (4/6/8/12); footer doesn't butt against BottomNav on mobile
       - **F. Responsive breakpoints** — Header sticky on scroll; BottomNav fixed on scroll (mobile); long-text overflows show at 375px first
       - **G. Theming / dark mode** — nighttime-bg (.nighttime-bg) text readability — only relevant if audit hits a night-view diary screen

    **Step C — Phase 5/6/7 file delta scan (additional check, beyond skill scope):**

    Run aggregate physical-CSS grep across ALL components (NOT just the 3 Phase 7 files); this expands the Phase 7 spec's narrow scope to catch any drift across the chrome + diary surfaces:
    ```bash
    grep -rnE "(^|[^a-zA-Z-])(ml-|mr-|pl-|pr-|left-|right-|border-l-|border-r-|text-left|text-right)[0-9]" src/components/ src/app/ 2>/dev/null | \
      grep -v "rtl:" | \
      grep -vE "(ms-|me-|ps-|pe-|start-|end-)" > /tmp/phase8-physical-css-scan.txt
    cat /tmp/phase8-physical-css-scan.txt
    ```

    Pre-existing physical-CSS allowlist (verified at planner time; do NOT blindly migrate, audit visually to confirm RTL still works):
    - `src/components/onboarding/OnboardingFlow.tsx:~220` — `<Calendar size={18} className="absolute left-3.5 top-1/2 ...` (UI-SPEC §"Step 3" defers)
    - `src/components/export/DaySummaryCard.tsx:41` — `<span className="text-ipc-500 font-medium ml-2">{dayLabel}</span>`
    - `src/components/export/DaySummaryCard.tsx:57,71` — `<span className="... ml-0.5">{volumeUnit}</span>`
    - `src/app/[locale]/LandingContent.tsx:111` — `<PlayCircle size={20} className="mr-2" />`
    - `src/components/layout/BottomNav.tsx:49` — `fixed bottom-0 left-0 right-0` (full-width chrome — semantically RTL-neutral, do not migrate)
    - `src/components/ui/Toast.tsx:25` — `fixed bottom-24 left-4 right-4` (centered toast — semantically RTL-neutral)
    - `src/components/ui/BottomSheet.tsx:150` — `absolute bottom-0 left-0 right-0` (full-width sheet — semantically RTL-neutral)
    - `src/components/diary/TimelineView.tsx:304` — `ml-6` indent on observation paragraph (check ar render at 375px; if it visually breaks, fix; otherwise log)
    - `src/components/diary/TimelineView.tsx:648` — `absolute -bottom-0 left-0 right-0` (full-width — RTL-neutral)
    - `src/components/diary/LogVoidForm.tsx:336`, `src/components/diary/LogDrinkForm.tsx:322` — `absolute left-1/2 -top-3 ... -translate-x-1/2` (centered chip — RTL-symmetric)
    - `src/components/diary/LogVoidForm.tsx:455`, `src/components/diary/LogDrinkForm.tsx:299`, `src/components/diary/LogLeakForm.tsx:307` — `w-full px-3 py-2.5 pr-11` (`pr-11` provides space for icon — check ar render, fix if visually broken with `pe-11`)
    - `src/components/diary/Day2ReminderCard.tsx:98` — `<p className="... pr-7">` (check ar render)

    For each allowlist entry: open the affected route at 375px in ar via Chrome MCP, visually confirm the rendering is correct. If correct → log as `[deferred-pre-phase-5] [allowlisted]`. If broken → fix in this plan (Step E batch).

    **Step D — Triage findings per skill §"Step 4" severity scoring:**

    For each finding, assign one of:
    - **BLOCKER** = breaks the diary flow OR makes content unreadable. Must close in this plan.
    - **MAJOR** = visible regression vs Phase 4 baseline. Must close in this plan.
    - **MINOR** = polish nit. Log to walkthrough_findings.md and defer to a future polish cycle.

    Apply the cutoff per CONTEXT §"Done criterion": if `git log --since "2026-05-14" -- {file}` shows the file was modified during Phases 5/6/7 AND the finding was introduced there → BLOCKER or MAJOR (close this plan). If the file was NOT modified since 2026-05-14 → tag as `[deferred-pre-phase-5]` and log without blocking.

    **Step E — Fix BLOCKERS + MAJORS in place per skill §"Step 5" + skill §"Common patterns":**

    For each BLOCKER/MAJOR, apply the fix per the skill's pattern guide:
    - Physical → logical: `ml-3` → `ms-3`, `pl-5` → `ps-5`, `text-left` → `text-start`, `right-0` → `end-0`, `left-0` → `start-0`, `border-l-` → `border-s-`
    - Inline `style={{ left: x }}` → `style={{ insetInlineStart: x }}`
    - `translateX(-50%)` centering: add `data-rtl-flip` attribute + globals.css rule `[dir="rtl"] [data-rtl-flip="x"] { transform: translateX(50%); }`
    - Long-text overflow: add `flex-wrap`, reduce `px-` on chips, or use `text-pretty` / `line-clamp-2`
    - Icon mirror: wrap chevron in `<span className="rtl:scale-x-[-1] inline-block">`

    Each fix is a SEPARATE atomic commit with message:
    `fix(08): {failure-mode-letter} — {one-line description} ({file}:{line})`

    Example: `fix(08): A — TimelineView observation indent migrated ml-6 → ms-6 for ar rendering (src/components/diary/TimelineView.tsx:304)`

    **Step F — Re-screenshot to verify per skill §"Step 6":**

    After each fix, re-render the affected (route, locale, width) screen and confirm the failure mode is gone. Re-screenshot for the human-verify package.

    **Step G — Update `walkthrough_findings.md`:**

    Append a new section at the top of the "Open issues" list:

    ```markdown
    ## Phase 8 visual-qa audit — 2026-05-17

    ### NEW findings (introduced by Phases 5–7; closed by Phase 8)

    - [2026-05-17] [phase8] [closed] [{locale}] [{route}] {one-line description} — fix commit {sha}
    (one entry per BLOCKER/MAJOR closed)

    ### NEW findings (pre-existing concerns surfaced for first time; closed by Phase 8)

    (zero or more entries)

    ### MINOR findings (logged, deferred to future polish cycle)

    - [2026-05-17] [phase8] [minor] [{locale}] [{route}] {description}

    ### Pre-Phase-5 historical findings (logged, NOT closed by Phase 8 per CONTEXT §"Out of scope")

    - [deferred-pre-phase-5] (entries from 2026-05-09 a11y scan etc., re-tagged)
    ```

    **Step H — Print audit summary to stdout:**

    ```
    === Phase 8 visual-qa audit (Plan 08-01 Task 1) ===
    Scope: SCOPED-DOWN 64 screens (3 priority × 6 × 3 + 5 secondary × 2 × 1)
    Cutoff for NEW findings: git --since "2026-05-14" (Phase 5 start)

    BLOCKERS closed: N
    MAJORS closed: N
    MINORS deferred: N
    Pre-Phase-5 historical: N (logged, not closed)

    Allowlisted pre-existing physical-CSS verified non-breaking in RTL: N entries
    Screenshots captured: 64 (matrix) + N (fix verification re-shots)

    Ready for Tasks 2-5 (C1/C2/C3/C4 carry-over closures).
    ```

    Server cleanup at task end: `pkill -f "next dev"` (the dev server stays running through the subsequent fix tasks if useful; explicit cleanup happens in Task 6).
  </action>

  <acceptance_criteria>
    - Dev server boots on port 3050; `curl http://localhost:3050/en` returns 200.
    - All 64 screens captured in the matrix (3 priority routes × 6 locales × 3 widths + 5 secondary routes × 2 locales × 1280px). Screenshot count ≥ 60 (allows for 4-screen attrition due to render delays / 404s on secondary routes that may not be populated in all locales).
    - All BLOCKER + MAJOR findings have a corresponding fix commit in git log (1 commit per finding; commit message starts with `fix(08): {A|B|C|D|E|F|G} — `).
    - Aggregate physical-CSS grep across ALL components (Step C) is captured to `/tmp/phase8-physical-css-scan.txt`; allowlist entries verified in ar render, non-allowlisted entries either closed by fix commit OR explicitly tagged as `[deferred-pre-phase-5]`.
    - `walkthrough_findings.md` updated with a `## Phase 8 visual-qa audit — 2026-05-17` section grouping NEW (closed), NEW (surfaced for first time), MINOR (deferred), pre-Phase-5 historical (logged).
    - Audit summary printed to stdout per Step H.
    - For BLOCKER/MAJOR fixes: TypeScript clean (`npx tsc --noEmit` exit 0), ESLint clean (`npx eslint src/` exit 0), Vitest baseline preserved (`npx vitest run` exit 0).
  </acceptance_criteria>

  <verify>
    <automated>test -f /tmp/phase8-physical-css-scan.txt && npx tsc --noEmit && npx eslint src/ 2>&1 | grep -E "(error|✖)" | grep -v "warning" | wc -l | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -q "Phase 8 visual-qa audit — 2026-05-17" .claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md</automated>
  </verify>

  <done>
    visual-qa scoped 64-screen matrix audit complete. All BLOCKER + MAJOR findings closed with atomic per-fix `fix(08): {letter} — ...` commits. Allowlisted pre-existing physical CSS verified non-breaking in RTL (or fixed if broken). MINOR findings logged for future polish cycle. Pre-Phase-5 historical findings re-tagged `[deferred-pre-phase-5]`. `walkthrough_findings.md` updated with the audit section. TypeScript + ESLint + Vitest clean. Ready to close C1/C2/C3/C4 in Tasks 2–5.
  </done>
</task>

<task type="auto">
  <name>Task 2: C1 — Render summary `<h1>` outside the `useStoreHydrated()` gate so it appears in pre-hydration static `out/{locale}/summary.html` for SEO</name>

  <read_first>
    - src/app/[locale]/summary/page.tsx (FULL — current shape: H1 at line 114 INSIDE the `if (!isComplete)` branch + the main render; both branches gate behind `if (!hydrated) return <spinner/>` at line 67)
    - messages/en.json key `summary.heroTitle` → "You did it. Three days, all noted." (current copy; locked, do NOT change)
    - .planning/phases/08-cross-locale-visual-qa-polish/08-CONTEXT.md §"Specific Ideas" → "C1 — H1 hydration race on /summary" — 3 options listed; recommended option (b): generic SEO-friendly H1 outside hydration gate, dynamic subtitle inside
    - .claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md (existing entries `[summary] Diary tab link to /summary not visible — seed may not have hydrated` confirm the hydration race is real and observable)
    - .planning/phases/05-layout-foundation-appshell-chrome/05-CONTEXT.md §"SEO invariants" (the SEO surface MUST contain ≥ 1 `<h1>` on every locale's static HTML)
    - .planning/phases/07-onboarding-summary-surfaces/07-04-PLAN.md §"H1 grep on out/{locale}.html is INFORMATIONAL not BLOCKER" — Phase 7's deferral that THIS plan closes (Phase 7 grep'd the landing page `out/{locale}.html`; Phase 8 closes by ensuring `out/{locale}/summary.html` has an SSR H1)
  </read_first>

  <files>src/app/[locale]/summary/page.tsx</files>

  <action>
    Apply CONTEXT recommended option (b): render a generic SEO-friendly `<h1>` OUTSIDE the `if (!hydrated) return <spinner/>` gate, so the `<h1>` element is part of the server-rendered output and appears in `out/{locale}/summary.html` regardless of hydration state. The post-hydration code continues to render the celebration H1 with the dynamic copy; the SEO H1 sits structurally above (or replaces the loading-spinner branch's lack of H1).

    **Concrete change pattern (do not paste verbatim — apply per current file shape):**

    Currently (line 67–73):
    ```
    if (!hydrated) {
      return (
        <div className="flex items-center justify-center py-24 bg-surface">
          <div className="w-10 h-10 rounded-full border-3 border-ipc-200 border-t-ipc-500 animate-spin" />
        </div>
      );
    }
    ```

    New shape: replace the bare spinner with a `<Container>` wrapping a static `<h1>` + the spinner, so the H1 ALWAYS renders at SSR time. Use the existing `summary.heroTitle` key for copy continuity (no new i18n key — ZERO new keys per CONTEXT §"i18n + RTL contract").

    ```
    if (!hydrated) {
      return (
        <Container variant="default" as="div" className="pt-12 text-center">
          <h1 className="text-2xl md:text-4xl font-bold text-ipc-950 text-balance leading-tight px-4 md:px-0 mb-6">
            {t('heroTitle')}
          </h1>
          <div className="flex items-center justify-center py-12">
            <div className="w-10 h-10 rounded-full border-3 border-ipc-200 border-t-ipc-500 animate-spin" />
          </div>
        </Container>
      );
    }
    ```

    The post-hydration render path (the existing line 114 `<h1>`) remains UNCHANGED — keep the post-hydration H1 with the same copy + same className (Plan 07-02's `text-2xl md:text-4xl ... px-4 md:px-0` is locked, don't touch).

    **Why this works:**
    - Pre-hydration: `<h1>summary.heroTitle</h1>` renders SERVER-SIDE (next-intl messages are loaded server-side via `getRequestConfig`); the H1 is in the static HTML.
    - Post-hydration: the spinner branch is skipped; the main render path's `<h1>` renders (same copy, same styling); the user sees no copy change.
    - No FOUC: the copy is identical pre and post hydration, so no flash of different content.
    - SEO surface: every `out/{locale}/summary.html` now contains ≥ 1 `<h1>` element with the heroTitle copy → grep returns ≥ 1.

    **What NOT to change:**
    - The `if (!diaryStarted) return null;` redirect-style branch at line 75 (different concern; that's the legitimately-no-summary state)
    - The `if (!isComplete) return <locked-state>` branch at line 77–87 (already has its own `<h2>` with `diaryLocked` key — no H1 there because the user hasn't earned summary access yet; this branch deliberately uses H2)
    - The main return path at line 89+ (Plan 07-02's typography decisions are locked)

    **i18n parity check:** verify `summary.heroTitle` exists in all 6 locale JSON files:
    ```bash
    for l in en fr es pt zh ar; do
      jq -r '.summary.heroTitle // "MISSING"' messages/$l.json | head -c 40
      echo " ← $l"
    done
    ```
    Expected: all 6 return a non-empty translated string (zero new keys; this is just a sanity check on the existing key).

    **SEO regression verification:** build the static export and grep for `<h1>` count in each `out/{locale}/summary.html`:
    ```bash
    npm run build 2>&1 | tail -20
    for l in en fr es pt zh ar; do
      h1Count=$(grep -c '<h1' out/$l/summary.html 2>/dev/null || echo 0)
      echo "$l: H1 count = $h1Count"
    done
    ```
    Expected: each locale reports H1 count ≥ 1 (was 0 in Phase 7 informational check; now ≥ 1).

    **Atomic commit:** `fix(08): C1 — render summary H1 outside hydration gate for SEO (src/app/[locale]/summary/page.tsx)`

    Mobile invariant check: at 375px in EN, the loading-state H1 + spinner should render briefly during the first paint (matches the same H1 that renders post-hydration). The user will see the H1 immediately + spinner below it. After hydration the spinner disappears + the full summary renders. ZERO mobile regression — the user gets the same H1 they would have seen post-hydration, just earlier.
  </action>

  <acceptance_criteria>
    - `src/app/[locale]/summary/page.tsx` modified: the `if (!hydrated)` branch now wraps the spinner in a `<Container>` with an `<h1>` rendering `t('heroTitle')` before the spinner.
    - The post-hydration H1 (Plan 07-02's line ~114) is UNCHANGED — same copy, same className.
    - No new i18n keys: `messages/en.json` has the same number of keys as before this task. `for l in en fr es pt zh ar; do jq 'paths(scalars) | join(".")' messages/$l.json | sort | wc -l; done | sort -u | wc -l` returns 1 (parity preserved).
    - `npm run build` exits 0.
    - For each locale, `grep -c '<h1' out/$l/summary.html` returns ≥ 1 (was 0 in Phase 7 informational check; now ≥ 1).
    - TypeScript clean: `npx tsc --noEmit` exits 0.
    - ESLint clean on the modified file: `npx eslint src/app/'[locale]'/summary/page.tsx` exits 0.
    - Vitest baseline preserved: `npx vitest run` exits 0.
    - Commit message exactly: `fix(08): C1 — render summary H1 outside hydration gate for SEO`
  </acceptance_criteria>

  <verify>
    <automated>npm run build > /tmp/c1-build.log 2>&1 && for l in en fr es pt zh ar; do count=$(grep -c '<h1' out/$l/summary.html 2>/dev/null || echo 0); [ "$count" -ge "1" ] || { echo "FAIL: $l H1 count = $count"; exit 1; }; done && npx tsc --noEmit && git log -1 --pretty=%s | grep -q "fix(08): C1"</automated>
  </verify>

  <done>
    C1 closed: summary `<h1>` renders outside the `useStoreHydrated()` gate via the loading-state Container; pre-hydration `out/{locale}/summary.html` for all 6 locales contains ≥ 1 `<h1>` element with the `summary.heroTitle` copy. Post-hydration render path unchanged (Plan 07-02 decisions preserved). Zero new i18n keys. Build clean. Mobile + RTL preserved. Atomic commit pushed.
  </done>
</task>

<task type="auto">
  <name>Task 3: C2 — PrivacyNotice no longer overlaps the QuickLogFAB or bottom-of-page content at any viewport in any of 6 locales</name>

  <read_first>
    - src/components/layout/PrivacyNotice.tsx (FULL — currently `fixed inset-x-0 bottom-20 z-40` at line 38)
    - src/components/layout/AppShell.tsx (FULL — currently renders `<PrivacyNotice />` after `<BottomNav />` at line 17–18; the `<div className="pb-24 md:pb-0" aria-hidden />` at line 16 provides bottom-padding spacer for mobile)
    - src/components/diary/QuickLogFAB.tsx (FULL — currently positioned `end-5 md:end-[...]` + `bottom-6/24 md:bottom-8` at lines 56–59; the FAB sits at bottom-24 (96px) when nav is visible, drops to bottom-6 (24px) when nav hides on scroll)
    - src/components/layout/BottomNav.tsx (FULL — currently `fixed bottom-0 left-0 right-0 z-30 md:hidden` at line 49; ~60px tall with safe-bottom)
    - src/components/ui/Toast.tsx (line 25 — `fixed bottom-24 left-4 right-4 z-50` — note the Toast also lives in the bottom-stack; verify it doesn't conflict with PrivacyNotice during the audit)
    - .planning/phases/08-cross-locale-visual-qa-polish/08-CONTEXT.md §"Specific Ideas" → "C2 — PrivacyNotice overlap" (3 candidate approaches listed)
  </read_first>

  <files>
    src/components/layout/PrivacyNotice.tsx
    src/components/layout/AppShell.tsx
  </files>

  <action>
    **Verify the overlap first.** Open the dev server (already running from Task 1) and visit `/en` at 375px / 768px / 1280px with localStorage cleared (so PrivacyNotice renders). Take a screenshot. Inspect the stacking:
    - PrivacyNotice: `fixed bottom-20 z-40` = sits 80px above viewport bottom
    - QuickLogFAB (when nav visible): `bottom-24 md:bottom-8` = 96px above bottom on mobile, 32px on desktop
    - BottomNav: `bottom-0` = 0px, ~60px tall
    - Toast: `bottom-24` z-50 = same vertical position as FAB

    **The overlap matrix:**
    - Mobile (375px): PrivacyNotice (80px) + FAB (96px) = FAB is 16px ABOVE PrivacyNotice → likely fine on mobile; but PrivacyNotice's card is ~80–100px tall (it CAN extend up to ~180px from bottom) → THIS overlaps the FAB at 96px when its content is more than 16px tall.
    - Desktop (1280px): PrivacyNotice (80px) + FAB at md:bottom-8 (32px) = FAB BELOW PrivacyNotice → FAB hidden under PrivacyNotice card.
    - BottomNav (mobile only): bottom-0 to 60px → PrivacyNotice at bottom-20 (80px) sits ABOVE the nav → no overlap there.

    **Confirmed:** the FAB and PrivacyNotice DO overlap at desktop (md+) because the FAB drops to md:bottom-8 (32px) while PrivacyNotice stays at bottom-20 (80px). The FAB sits BELOW PrivacyNotice on desktop — invisible behind it.

    **Fix approach (planner pick from CONTEXT §"C2" Option 2 — converted to a positioning shift, not a layout overhaul):**

    Shift PrivacyNotice to sit ABOVE the FAB on desktop, and ensure it doesn't overlap the FAB on mobile either. The simplest correct fix:

    **PrivacyNotice positioning change (line 38):**
    ```
    // Was:
    className="fixed inset-x-0 bottom-20 z-40 px-3 sm:px-6 pointer-events-none"
    // To:
    className="fixed inset-x-0 bottom-44 md:bottom-28 z-40 px-3 sm:px-6 pointer-events-none"
    ```

    Math:
    - Mobile (375px): PrivacyNotice at bottom-44 (176px) sits ABOVE FAB bottom-24 (96px) — clear 80px gap, no overlap even with the card's full ~100px height.
    - Desktop (1280px): PrivacyNotice at bottom-28 (112px) sits ABOVE FAB md:bottom-8 (32px) — clear 80px gap.
    - When BottomNav hides on scroll (mobile), FAB drops to bottom-6 (24px); PrivacyNotice at bottom-44 (176px) still sits well above — no overlap.

    **Why NOT convert to in-flow with sticky-show-on-scroll** (CONTEXT Option 2 alt): too invasive — the privacy notice is currently a dismissible card that shows once + saves to localStorage; converting it to in-flow would require restructuring the layout architecture (place it after content in AppShell, change z-stacking model). The bottom-shift fix is 1-line, deterministic, and preserves the existing dismiss-once UX.

    **z-index sanity check:** PrivacyNotice stays z-40, FAB stays z-50, BottomNav z-30, Toast z-50. Stacking order top-to-bottom: Toast/FAB (z-50) > PrivacyNotice (z-40) > BottomNav (z-30). When PrivacyNotice shows, the FAB SHOULD still be reachable (it's higher z + visually positioned above PrivacyNotice now). Verify with a mouse-click on the FAB while PrivacyNotice is visible — should fire the FAB onClick, not the PrivacyNotice onClick.

    **i18n check:** zero new keys (positioning shift only).

    **Verification at all 6 locales × 3 viewports:**

    Walk the matrix via Chrome MCP:
    - For each locale × 375/768/1280px: clear localStorage (so PrivacyNotice renders), navigate to `/<locale>/diary/day/1` (the surface where FAB + PrivacyNotice coexist), screenshot.
    - Verify: PrivacyNotice card visible at the new position; FAB visible BELOW it without occlusion; clicking the FAB fires the FAB action (not the PrivacyNotice card).
    - For pt + ar locales: confirm long-translation copy in PrivacyNotice doesn't exceed `max-w-md` (the existing constraint) — if it overflows, that's a separate finding logged in Task 1, not part of C2.

    **AppShell.tsx**: no change needed. The pb-24 md:pb-0 spacer at line 16 is for BottomNav clearance, not PrivacyNotice. Verify by reading the file in full first.

    **Atomic commit:** `fix(08): C2 — PrivacyNotice positioned above FAB at all viewports (no overlap) (src/components/layout/PrivacyNotice.tsx)`

    Mobile invariant: the new bottom-44 ≥ old bottom-20 on mobile, so the PrivacyNotice card sits HIGHER in the viewport on mobile (more breathing room from bottom). Not a regression — a strict improvement.
  </action>

  <acceptance_criteria>
    - `src/components/layout/PrivacyNotice.tsx` line 38 modified: `bottom-20` → `bottom-44 md:bottom-28`.
    - `src/components/layout/AppShell.tsx` UNCHANGED (verified by `git diff src/components/layout/AppShell.tsx` returning empty).
    - At 1280px in EN with cleared localStorage: PrivacyNotice card visible at ~112px from viewport bottom; QuickLogFAB visible at ~32px from viewport bottom; clear visual gap between them.
    - At 375px in EN with cleared localStorage: PrivacyNotice card visible at ~176px from viewport bottom; QuickLogFAB visible at ~96px from viewport bottom; clear visual gap.
    - Verified across all 6 locales at 375px + 768px + 1280px via Chrome MCP screenshots (18 screenshots total; saved under `/tmp/c2-screenshots/`).
    - FAB click-through works while PrivacyNotice is visible (no z-index regression).
    - Zero new i18n keys.
    - TypeScript + ESLint + Vitest clean.
    - Commit message: `fix(08): C2 — PrivacyNotice positioned above FAB at all viewports (no overlap)`
  </acceptance_criteria>

  <verify>
    <automated>grep -q "bottom-44 md:bottom-28" src/components/layout/PrivacyNotice.tsx && [ -z "$(git diff src/components/layout/AppShell.tsx)" ] && npx tsc --noEmit && git log -1 --pretty=%s | grep -q "fix(08): C2"</automated>
  </verify>

  <done>
    C2 closed: PrivacyNotice positioning shifted from `bottom-20` to `bottom-44 md:bottom-28` so it sits ABOVE the QuickLogFAB at all viewports (mobile + tablet + desktop). Verified across 6 locales × 3 viewports = 18 screenshots showing clear gap with no overlap. AppShell unchanged. Z-stacking preserved. FAB remains click-through-able with PrivacyNotice visible. Zero new i18n keys. Atomic commit pushed.
  </done>
</task>

<task type="auto">
  <name>Task 4: C3 — Gate DayPageClient redirect logic on `useStoreHydrated()` to eliminate the deep-link redirect race</name>

  <read_first>
    - src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx (FULL — currently has 2 unguarded redirect useEffects at lines 129–133 and 136–140; `useDiaryStore()` is called at line 42 without hydration check)
    - src/lib/store.ts (verify `useStoreHydrated` is exported — used in LandingContent.tsx line 41 + summary/page.tsx line 29)
    - src/app/[locale]/LandingContent.tsx (FULL — reference pattern: line 41 `const hydrated = useStoreHydrated();` line 86 `if (!hydrated) return <loading-state>` — THE pattern this fix replicates)
    - src/app/[locale]/summary/page.tsx (lines 29 + 50–54 + 67–73 — reference pattern: hydrated check gates redirects + render)
    - .planning/phases/08-cross-locale-visual-qa-polish/08-CONTEXT.md §"Specific Ideas" → "C3 — DayPageClient redirect race" — recommended pattern: "gate the redirect on hydration; render a stable loading skeleton until hydrated"
    - .claude/projects/-Users-zhen-bladderdiary-patient/memory/walkthrough_findings.md (entries `[deep-flow] [deepLinkSummary] Deep-link redirect race regressed` confirm the race is real)
  </read_first>

  <files>src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx</files>

  <action>
    Mirror the LandingContent + summary hydration-gate pattern in DayPageClient. The current code (line 42) reads from `useDiaryStore()` and immediately makes routing decisions in two useEffects:

    ```tsx
    // Current (lines 128–140) — UNGUARDED, fires on initial empty state before hydration:
    useEffect(() => {
      if (!diaryStarted) {
        router.replace('/');
      }
    }, [diaryStarted, router]);

    useEffect(() => {
      if (diaryStarted && !prevDayComplete) {
        router.replace(`/diary/day/${dayNumber - 1}`);
      }
    }, [diaryStarted, prevDayComplete, dayNumber, router]);
    ```

    **The race:** Zustand's `persist` middleware hydrates asynchronously. On the first render after a deep-link to `/diary/day/2`, `diaryStarted` is initially `false` (default state) before localStorage hydrates. The redirect useEffect fires → router.replace('/'). When localStorage hydrates a moment later, `diaryStarted` becomes `true`, but the route has already changed. The user lands on the landing page instead of Day 2 — the bug the walkthrough findings log.

    **The fix:**

    1. Import `useStoreHydrated`:
    ```tsx
    import { useDiaryStore, useStoreHydrated } from '@/lib/store';
    ```

    2. After the existing `useDiaryStore()` destructure at line 42–50, add:
    ```tsx
    const hydrated = useStoreHydrated();
    ```

    3. Gate the redirect useEffects on `hydrated`:
    ```tsx
    // Redirect to landing if diary not started — gated on hydration to avoid
    // a race where the persist middleware hasn't yet loaded localStorage and
    // diaryStarted reads as the default `false` from the unhydrated store,
    // causing a wrong redirect when the user deep-links to /diary/day/N.
    // Mirrors the same pattern in src/app/[locale]/summary/page.tsx and
    // src/app/[locale]/LandingContent.tsx.
    useEffect(() => {
      if (!hydrated) return;
      if (!diaryStarted) {
        router.replace('/');
      }
    }, [hydrated, diaryStarted, router]);

    // Redirect to previous day if it's not complete yet — same hydration gate.
    useEffect(() => {
      if (!hydrated) return;
      if (diaryStarted && !prevDayComplete) {
        router.replace(`/diary/day/${dayNumber - 1}`);
      }
    }, [hydrated, diaryStarted, prevDayComplete, dayNumber, router]);
    ```

    4. Replace the existing early-return at line 302 (`if (!diaryStarted || !prevDayComplete) return null;`) with a hydration-aware loading state:

    ```tsx
    // Until hydration finishes, show a non-committal loading state matching
    // the Suspense fallback used in landing + summary. Avoids flash of either
    // the redirect target or the page content.
    if (!hydrated) {
      return (
        <div className="flex items-center justify-center py-24 bg-surface">
          <div className="w-10 h-10 rounded-full border-3 border-ipc-200 border-t-ipc-500 animate-spin" />
        </div>
      );
    }

    if (!diaryStarted || !prevDayComplete) return null;
    ```

    5. The auto-open void form useEffect at line 110–117 (the `searchParams.get('add') === 'void'` shortcut) and the track-pageview useEffect at line 121–126 SHOULD ALSO gate on hydration, because they depend on `diaryStarted` and `prevDayComplete`:

    ```tsx
    useEffect(() => {
      if (!hydrated) return;
      if (autoOpenConsumed.current) return;
      if (!diaryStarted || !prevDayComplete) return;
      if (searchParams.get('add') === 'void' && canLogEntries) {
        autoOpenConsumed.current = true;
        setSheetMode('void');
      }
    }, [hydrated, diaryStarted, prevDayComplete, searchParams, canLogEntries]);

    useEffect(() => {
      if (!hydrated) return;
      if (diaryStarted && prevDayComplete && trackedDay.current !== dayNumber) {
        trackedDay.current = dayNumber;
        track('view_day', { day: dayNumber });
      }
    }, [hydrated, diaryStarted, prevDayComplete, dayNumber]);
    ```

    6. The night-view body class useEffect at line 143–152 does NOT need a hydration gate because `isNightView` is computed from `searchParams.get('view')` + `hasWakeTime` (which depends on store data); however, since the effect only mutates `document.body.classList` (idempotent class toggle), it's safe either way. Leave UNCHANGED to minimize churn.

    **Verification:**

    1. Build + serve:
    ```bash
    npm run build && (npx --yes serve out -l 4173 --no-clipboard &) ; sleep 3
    ```

    2. Deep-link test (Chrome MCP):
    - Clear localStorage.
    - Navigate to `http://localhost:4173/en/diary/day/2` (deep-link to Day 2 with NO prior diary state).
    - Verify: page shows the loading spinner briefly, then redirects to `/en/` (landing). No flicker between Day 2 content and landing.

    3. Deep-link to a valid mid-diary state:
    - Seed localStorage with a state where Day 1 is complete (bedtime set) and the user is on Day 2.
    - Deep-link `http://localhost:4173/en/diary/day/2`.
    - Verify: spinner briefly, then Day 2 view renders. No redirect.

    4. Deep-link to summary (the related concern from walkthrough_findings):
    - Seed localStorage with complete 3-day diary (bedtimes on all 3 days).
    - Deep-link `http://localhost:4173/en/summary`.
    - Verify: summary page renders correctly without bouncing to landing. (This was already gated in summary/page.tsx — verify the regression is gone now that DayPageClient redirects don't fire wrongly during related navigation flows.)

    5. Run the existing daily walkthrough spec at the deep-flow project to confirm the `deepLinkSummary` failure is resolved:
    ```bash
    npx playwright test e2e/deep-flow.spec.ts --reporter=line
    ```
    Expected: previously-failing `deepLinkSummary` test now PASSES.

    **Atomic commit:** `fix(08): C3 — gate DayPageClient redirects on store hydration (no flicker on deep-link)`

    Mobile invariant: the new loading skeleton renders the same way on mobile + desktop (it's the same spinner UI as LandingContent + summary). No visual regression — the loading state was previously a render-then-redirect flicker, now it's a stable spinner.
  </action>

  <acceptance_criteria>
    - `src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx`: `useStoreHydrated` imported from `@/lib/store`.
    - `hydrated` variable declared via `useStoreHydrated()` after the `useDiaryStore()` destructure.
    - Both redirect useEffects (lines ~129 and ~136) gated on `if (!hydrated) return;` at the top of the effect body.
    - The auto-open void form useEffect (line ~110) and the track-pageview useEffect (line ~121) also gated on hydration.
    - A new loading skeleton block (matching the spinner pattern from LandingContent line ~86 and summary line ~67) renders when `!hydrated`, placed before the existing `if (!diaryStarted || !prevDayComplete) return null;` early-return.
    - Deep-link to `/diary/day/2` with cleared localStorage: spinner → redirect to `/` (no flicker between the two).
    - Deep-link to `/diary/day/2` with valid mid-diary state seeded: spinner → Day 2 view (no flicker, no wrong redirect).
    - `npx playwright test e2e/deep-flow.spec.ts --reporter=line` exits 0 (the `deepLinkSummary` test passes — was failing per walkthrough_findings).
    - TypeScript + ESLint + Vitest clean.
    - Commit message: `fix(08): C3 — gate DayPageClient redirects on store hydration (no flicker on deep-link)`
  </acceptance_criteria>

  <verify>
    <automated>grep -c "useStoreHydrated" src/app/'[locale]'/diary/day/'[dayNumber]'/DayPageClient.tsx | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c "if (!hydrated) return" src/app/'[locale]'/diary/day/'[dayNumber]'/DayPageClient.tsx | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && npx tsc --noEmit && git log -1 --pretty=%s | grep -q "fix(08): C3"</automated>
  </verify>

  <done>
    C3 closed: DayPageClient now uses `useStoreHydrated()` to gate all 4 useEffects that depend on store state (2 redirects + auto-open + track-pageview), AND renders a stable loading spinner until hydration completes. Deep-links to `/diary/day/N` no longer race-flicker; either resolve to the day view (if state exists) OR cleanly redirect to landing (if no state). `e2e/deep-flow.spec.ts` passes (was failing per walkthrough_findings). Mirrors the LandingContent + summary pattern exactly. Atomic commit pushed.
  </done>
</task>

<task type="auto">
  <name>Task 5: C4 — QuickLogFAB speed-dial chips bumped to `min-h-[44px]` (Boomer-safe override 1 correctness fix)</name>

  <read_first>
    - src/components/diary/QuickLogFAB.tsx (FULL — currently 3 speed-dial buttons at lines 63–74 (drink), 76–87 (leak), 89–100 (void); each uses `flex items-center justify-between gap-3 min-w-[8rem] ps-4 pe-3 py-2.5 rounded-full ...`; py-2.5 = 10px each side = 20px vertical padding; the icon disc inside is `w-10 h-10` = 40px; rendered button height ≈ max(40, 20 + text-line-height) ≈ 40–48px)
    - .planning/phases/05-layout-foundation-appshell-chrome/05-CONTEXT.md §"Boomer-safe overrides" Override 1 (44px hit-target minimum on EVERY interactive element, mobile AND desktop)
    - .planning/phases/07-onboarding-summary-surfaces/07-01-PLAN.md (the precedent: 2 back-pill buttons bumped from `min-h-[40px]` to `min-h-[44px]` in Plan 07-01 — same correctness class as this fix)
    - .planning/phases/08-cross-locale-visual-qa-polish/08-CONTEXT.md §"Specific Ideas" → "C4 — QuickLogFAB speed-dial 44px" — options listed
  </read_first>

  <files>src/components/diary/QuickLogFAB.tsx</files>

  <action>
    Apply the simplest deterministic fix per CONTEXT §"C4": add `min-h-[44px]` to each of the 3 speed-dial chip buttons.

    **Why `min-h-[44px]` not `py-3` bump:**
    - `min-h-[44px]` explicitly states the hit-target floor and is self-documenting; matches the Phase 7 back-pill precedent (Plan 07-01 used `min-h-[44px]`).
    - `py-3` (12px each side = 24px total) PLUS the existing `w-10 h-10` (40px) icon would yield max(40, 24+text) ≈ 44–52px; works, but is less explicit and might break if the icon size changes later.
    - `min-h-[44px]` lets the existing `py-2.5` flexbox layout compute naturally, and the min-h ensures a floor of 44px regardless of content height.

    **The 3 changes** (lines 67, 80, 93 — the className strings on the drink/leak/void chip buttons):

    Drink chip (line 67) — current:
    ```
    className="flex items-center justify-between gap-3 min-w-[8rem] ps-4 pe-3 py-2.5 rounded-full bg-drink
      text-white shadow-lg active:scale-[0.95] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface transition-transform"
    ```

    Drink chip new (add `min-h-[44px]` after `min-w-[8rem]`):
    ```
    className="flex items-center justify-between gap-3 min-w-[8rem] min-h-[44px] ps-4 pe-3 py-2.5 rounded-full bg-drink
      text-white shadow-lg active:scale-[0.95] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface transition-transform"
    ```

    Same single-token addition (`min-h-[44px]` after `min-w-[8rem]`) to the leak chip (line 80) and void chip (line 93).

    **What does NOT change:**
    - The main FAB toggle button at line 110 is `w-16 h-16` = 64px (already > 44px) — no change.
    - The icon disc inside the chips (`w-10 h-10` at lines 71/84/97) stays 40px — the chip button bumps to 44px around it; the icon stays centered.
    - The fade-slide-up animation, the focus-visible rings, the active:scale, the transition-transform — all preserved.
    - The label text (`text-base font-semibold`) — unchanged.
    - Position classes (`fixed z-50 ... end-5 md:end-[max(...)] bottom-24 md:bottom-8`) — unchanged.

    **Mobile invariant carve-out** (per CONTEXT §"Inheritance" + Plan 07-01 precedent):

    This IS a mobile diff (the chip is +4px taller at 375px too), accepted as a Boomer-safe correctness fix — same precedent class as Plan 07-01's back-pill bump and Plan 05-04's NavLink bump. The diff applies at ALL viewports (mobile + tablet + desktop) because `min-h-[44px]` is not gated by a `md:` prefix. CONTEXT §"Inheritance" explicitly accepts this: "every fix preserves mobile bit-equivalence except correctness fixes that improve mobile (e.g., C4's 40→44px FAB bump improves mobile too — accepted carve-out, same precedent as Phase 7 back-pill + Phase 5 NavLink)."

    **Verification:**

    1. Open dev server, navigate to `/en/diary/day/1` at 375px (mobile).
    2. Click the main FAB to expand it.
    3. Inspect each of the 3 speed-dial chips via Chrome MCP `mcp__Claude_in_Chrome__find` or DevTools: computed `min-height` should be `44px`.
    4. Repeat at 1280px (desktop).
    5. Visually: the chips should still feel like the same compact pill, just slightly more comfortable to hit. The `w-10 h-10` icon disc inside still looks proportional.

    **Playwright assertion (optional inline test for future regression catch, but the dedicated Plan 08-02 spec is where this lives long-term):**
    ```ts
    await expect(page.getByTestId('fab-action-drink')).toHaveCSS('min-height', '44px');
    await expect(page.getByTestId('fab-action-leak')).toHaveCSS('min-height', '44px');
    await expect(page.getByTestId('fab-action-void')).toHaveCSS('min-height', '44px');
    ```

    **Atomic commit:** `fix(08): C4 — QuickLogFAB speed-dial min-h-[44px] (Boomer-safe override 1)`
  </action>

  <acceptance_criteria>
    - `src/components/diary/QuickLogFAB.tsx` lines 67, 80, 93 each have `min-h-[44px]` added to the className string (between `min-w-[8rem]` and `ps-4`).
    - `grep -c 'min-h-\[44px\]' src/components/diary/QuickLogFAB.tsx` returns 3 (one per chip).
    - No other lines modified in QuickLogFAB.tsx (verified by `git diff src/components/diary/QuickLogFAB.tsx` showing only the 3 className additions).
    - At 375px and 1280px via Chrome MCP: computed `min-height` on each of the 3 chip buttons is `44px`.
    - The main FAB toggle button (line ~110, `w-16 h-16`) is NOT modified.
    - The icon disc inside each chip (`w-10 h-10`) is NOT modified.
    - TypeScript + ESLint + Vitest clean.
    - Commit message: `fix(08): C4 — QuickLogFAB speed-dial min-h-[44px] (Boomer-safe override 1)`
  </acceptance_criteria>

  <verify>
    <automated>[ "$(grep -c 'min-h-\[44px\]' src/components/diary/QuickLogFAB.tsx)" = "3" ] && grep -q "w-16 h-16" src/components/diary/QuickLogFAB.tsx && npx tsc --noEmit && git log -1 --pretty=%s | grep -q "fix(08): C4"</automated>
  </verify>

  <done>
    C4 closed: 3 QuickLogFAB speed-dial chips bumped to `min-h-[44px]` (Boomer-safe override 1). Mobile diff accepted per CONTEXT §"Inheritance" (same precedent class as Phase 7 back-pill + Phase 5 NavLink fixes). Main FAB toggle (already 64px) untouched. Icon discs unchanged. Verified via Chrome MCP computed CSS at 375px + 1280px. Atomic commit pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human-verify checkpoint — review the visual-qa audit findings + the 4 closed carry-overs + run final regression gates (daily walkthrough + e2e/deep-flow + e2e/walkthrough)</name>

  <what-built>
    Tasks 1–5 deliver:

    **Task 1 — visual-qa scoped 64-screen matrix:**
    - 54 priority screens (`/`, `/diary/day/1`, `/summary` × 6 locales × 3 widths)
    - 10 secondary screens (5 routes × {en, ar} × 1280px)
    - Aggregate physical-CSS scan across ALL components (allowlisted entries verified non-breaking in RTL; new violations either fixed or tagged)
    - BLOCKER + MAJOR findings fixed inline with atomic `fix(08): {letter} — ...` commits
    - MINOR findings logged to `walkthrough_findings.md` for future polish cycle
    - Pre-Phase-5 historical findings re-tagged `[deferred-pre-phase-5]`

    **Task 2 (C1) — SUMMARY H1 hydration:**
    - `src/app/[locale]/summary/page.tsx` modified: H1 rendered in loading-state Container (uses existing `summary.heroTitle` key — zero new i18n keys)
    - Pre-hydration `out/{locale}/summary.html` now contains ≥ 1 `<h1>` for all 6 locales (was 0)
    - Post-hydration render path UNCHANGED (Plan 07-02 typography preserved)
    - Commit: `fix(08): C1 — render summary H1 outside hydration gate for SEO`

    **Task 3 (C2) — PrivacyNotice overlap:**
    - `src/components/layout/PrivacyNotice.tsx` line 38: `bottom-20` → `bottom-44 md:bottom-28`
    - No overlap with FAB or BottomNav at any viewport in any locale
    - AppShell unchanged; z-stacking preserved; FAB remains click-through-able with PrivacyNotice visible
    - Commit: `fix(08): C2 — PrivacyNotice positioned above FAB at all viewports (no overlap)`

    **Task 4 (C3) — DayPageClient redirect race:**
    - `src/app/[locale]/diary/day/[dayNumber]/DayPageClient.tsx`: `useStoreHydrated` imported; 4 useEffects gated on hydration (2 redirects + auto-open + track-pageview); loading spinner replaces immediate render-then-redirect flicker
    - Deep-links to `/diary/day/N` resolve cleanly (spinner → day view OR spinner → landing redirect)
    - `e2e/deep-flow.spec.ts` `deepLinkSummary` test PASSES (was failing per walkthrough_findings)
    - Commit: `fix(08): C3 — gate DayPageClient redirects on store hydration (no flicker on deep-link)`

    **Task 5 (C4) — QuickLogFAB speed-dial 44px:**
    - `src/components/diary/QuickLogFAB.tsx` lines 67/80/93: `min-h-[44px]` added to each of 3 speed-dial chips
    - Boomer-safe override 1 correctness fix (44px hit-target floor); mobile diff accepted per CONTEXT precedent class
    - Main FAB toggle (already 64px) untouched
    - Commit: `fix(08): C4 — QuickLogFAB speed-dial min-h-[44px] (Boomer-safe override 1)`

    **All inheritance preserved:**
    - Mobile-first PRIMACY (mobile bit-equivalent except C4's accepted 4px chip bump and the PrivacyNotice positioning shift which is a strict improvement in mobile breathing room)
    - Streamlined Cognition (8 principles): C1 makes state more visible pre-hydration; C2 removes the "FAB hidden under PrivacyNotice" silent state; C3 removes the redirect flicker (one obvious recovery step); C4 ensures hit-target is reachable
    - Boomer-safe overrides: C4 is override 1; C2 ensures FAB stays clickable; C3 removes the flicker that confuses non-tech-savvy users
    - Design DNA axes: no novel patterns introduced; all changes are className edits or 1-line positioning shifts
    - i18n + RTL: ZERO new keys; PrivacyNotice positioning is LTR/RTL-symmetric (`fixed inset-x-0 bottom-44`); all C1–C4 fixes are physical-CSS-free
  </what-built>

  <how-to-verify>
    1. **Review the visual-qa audit summary** from Task 1 stdout. Confirm:
       - BLOCKER count + MAJOR count from the scoped-down 64-screen matrix
       - All fix commits listed in `git log --oneline --since "2026-05-17 morning" --author "$(git config user.email)"`
       - MINOR findings logged to `walkthrough_findings.md` are reasonable to defer

    2. **Walk the 4 carry-over fixes in the live dev server** (`http://localhost:3050` if still running; otherwise re-boot per Task 1 Step 0):
       - **C1 verify:** open `/en/summary` (in incognito / cleared storage so summary is the locked state). Then via DevTools → Network throttle to "Slow 3G" → reload. You should see the `<h1>` "You did it. Three days, all noted." render IMMEDIATELY in the loading state, before the spinner finishes. Repeat for `/ar/summary` to confirm RTL works.
       - **C2 verify:** clear localStorage, open `/en/diary/day/1`. PrivacyNotice card should appear ~80px above the FAB (clearly separated). The FAB should be clickable. Test at 375px (mobile DevTools), 768px (tablet), 1280px (desktop). Repeat for `/ar/diary/day/1` to confirm RTL doesn't introduce overlap.
       - **C3 verify:** with cleared storage, deep-link `/en/diary/day/2` — should show spinner briefly then redirect to landing (no flash of "Day 2" content). Then seed `localStorage` via DevTools with a state that has Day 1 complete (the `STORE_KEY` from `e2e/helpers/fixtures.ts`), deep-link `/en/diary/day/2` — should show spinner briefly then render Day 2 (no flash of landing).
       - **C4 verify:** open `/en/diary/day/1` at 375px (mobile DevTools), click the main FAB to expand. Right-click one of the 3 speed-dial chips → Inspect. In DevTools computed-styles panel, verify `min-height: 44px`. Repeat at 1280px.

    3. **Run the final regression gates:**

       ```bash
       # Daily walkthrough — should still pass
       npx playwright test e2e/walkthrough.spec.ts --reporter=line

       # Deep-flow — the deepLinkSummary test should now PASS (was failing)
       npx playwright test e2e/deep-flow.spec.ts --reporter=line

       # Phase 5/6/7 verification specs — should still pass
       PW_TEST_MATCH='phase5-chrome\\.spec\\.ts' npx playwright test e2e/phase5-chrome.spec.ts --reporter=line
       PW_TEST_MATCH='phase6-keyboard\\.spec\\.ts' npx playwright test e2e/phase6-keyboard.spec.ts --reporter=line
       PW_TEST_MATCH='phase7-onboarding-summary\\.spec\\.ts' npx playwright test e2e/phase7-onboarding-summary.spec.ts --reporter=line

       # A11y scan — pre-Phase-5 findings remain logged but are not new
       npx playwright test e2e/a11y.spec.ts --reporter=line
       ```

    4. **Build verification:**
       ```bash
       npm run build
       # SEO regression check — every locale's H1 count should be ≥ 1 (C1 fix)
       for l in en fr es pt zh ar; do
         h1=$(grep -c '<h1' out/$l/summary.html 2>/dev/null || echo 0)
         echo "$l: H1 count = $h1"
       done
       ```
       Expected: each locale reports H1 count ≥ 1.

    5. **Confirm walkthrough_findings.md sanity:**
       - The pre-Phase-5 historical findings from 2026-05-09 should be re-tagged `[deferred-pre-phase-5]` (NOT closed by Phase 8 per scope).
       - The 2026-05-16 + 2026-05-17 `[summary] Diary tab link to /summary not visible` entries (C1 + C3 root cause) should be marked as `closed by Phase 8 — C1 + C3 fixes`.
       - Any NEW findings surfaced by the Phase 8 audit should either be `[closed]` (BLOCKERS + MAJORS) or `[minor]` (deferred to future polish).

    6. **Server cleanup:**
       ```bash
       pkill -f "next dev" 2>/dev/null
       pkill -f "serve out" 2>/dev/null
       ```
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 6 verification items above pass.
    Type "issues: {description}" if any carry-over fix has a visible regression, the visual-qa audit summary is unreasonable, or one of the regression gates fails. Issues route back to the relevant Task (1/2/3/4/5) for fix-up before Phase 8 ships.
  </resume-signal>
</task>

</tasks>

<verification>
  - TypeScript: `npx tsc --noEmit` exits 0.
  - ESLint: `npx eslint src/ e2e/` exits 0 (no errors; warnings ok).
  - Vitest: `npx vitest run` exits 0 (no regressions in the unit suite).
  - Build: `npm run build` exits 0; `out/` regenerated.
  - SEO regression (C1): for each locale, `grep -c '<h1' out/$l/summary.html` returns ≥ 1.
  - Daily walkthrough: `npx playwright test e2e/walkthrough.spec.ts --reporter=line` exits 0.
  - Deep-flow (C3 root cause): `npx playwright test e2e/deep-flow.spec.ts --reporter=line` exits 0; the `deepLinkSummary` test now passes.
  - Phase 5/6/7 verification specs: all still exit 0 (no regression from the Phase 8 fixes in their target surfaces).
  - i18n parity: `for l in en fr es pt zh ar; do jq 'paths(scalars) | join(".")' messages/$l.json | sort | wc -l; done | sort -u | wc -l` returns 1 (zero new keys; pre-existing parity preserved).
  - Aggregate physical-CSS guard: any non-allowlisted physical CSS introduced by Phases 5–7 closed by Task 1 fixes; allowlisted entries verified non-breaking in RTL.
  - 4 carry-over commits exist in git log with the prescribed messages:
    - `fix(08): C1 — render summary H1 outside hydration gate for SEO`
    - `fix(08): C2 — PrivacyNotice positioned above FAB at all viewports (no overlap)`
    - `fix(08): C3 — gate DayPageClient redirects on store hydration (no flicker on deep-link)`
    - `fix(08): C4 — QuickLogFAB speed-dial min-h-[44px] (Boomer-safe override 1)`
  - Walkthrough_findings.md: `## Phase 8 visual-qa audit — 2026-05-17` section present; 2026-05-16 `[summary]` + `[deepLinkSummary]` entries marked as closed; pre-Phase-5 historical entries re-tagged.
  - Human-verify checkpoint: user has typed "approved" (or an issue has routed back to a specific task).
  - All 6 ROADMAP §"Phase 8" success criteria met (per the goal-backward table in the objective).
</verification>

<success_criteria>
  Phase 8 visual-qa + 4 carry-over closure plan complete when:

  1. **visual-qa scoped-down 64-screen matrix** has been audited per `.claude/skills/visual-qa/SKILL.md` workflow; all BLOCKER + MAJOR findings closed with atomic `fix(08): {letter} — ...` commits; MINOR findings logged to `walkthrough_findings.md`; pre-Phase-5 historical findings re-tagged `[deferred-pre-phase-5]`.

  2. **C1 closed:** summary `<h1>` rendered outside the `useStoreHydrated()` gate; `out/{locale}/summary.html` for all 6 locales contains ≥ 1 `<h1>`; commit `fix(08): C1 — ...` exists.

  3. **C2 closed:** PrivacyNotice no longer overlaps QuickLogFAB at any viewport (`bottom-44 md:bottom-28`); verified visually across 6 locales × 3 widths; commit `fix(08): C2 — ...` exists.

  4. **C3 closed:** DayPageClient redirects gated on hydration; deep-links resolve without flicker; `e2e/deep-flow.spec.ts` `deepLinkSummary` test passes; commit `fix(08): C3 — ...` exists.

  5. **C4 closed:** 3 QuickLogFAB speed-dial chips have `min-h-[44px]`; Boomer-safe override 1 satisfied; commit `fix(08): C4 — ...` exists.

  6. **Daily walkthrough still passes** (`e2e/walkthrough.spec.ts` exit 0; no NEW findings in `walkthrough_findings.md`).

  7. **All inheritance preserved** (mobile-first PRIMACY, Streamlined Cognition, Boomer-safe overrides, Design DNA axes); ZERO new i18n keys; logical CSS only (or pre-existing physical CSS verified non-breaking + tagged).

  8. **Human-verify checkpoint approved** by the user (Task 6 resume-signal received).
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-locale-visual-qa-polish/08-01-SUMMARY.md` documenting:
- The audit scope chosen (scoped-down 64-screen) + rationale
- The BLOCKER + MAJOR finding counts + per-finding fix commits
- The 4 carry-over closures + their commits
- The MINOR + pre-Phase-5 findings deferred (for the future polish cycle)
- The final regression gate results (walkthrough, deep-flow, Phase 5/6/7 specs all green)
- The cross-reference to the Phase 8 ROADMAP success criteria (1–5 + carry-over closes)
</output>
</content>
</invoke>
---
plan_id: "06-09"
phase: 6
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/components/diary/DrinkTypePicker.tsx
  - src/components/diary/LeakTriggerPicker.tsx
  - src/components/diary/SensationPicker.tsx
  - src/components/ui/VolumeInput.tsx
requirements_addressed: ["DTUX-03"]
autonomous: true
---

# Plan 06-09: Add `focus-visible:` ring to all sub-picker tiles + Sensation help (?) toggle + VolumeInput tap-to-edit button — 4 surgical files, picker grids and data-testids PRESERVED, mobile zero pixel diff

<objective>
Per 06-UI-SPEC §"Sub-Picker Specs" (DrinkTypePicker / LeakTriggerPicker / SensationPicker tables — locked) + §"Open Questions" → Q8 (locked: YES, add focus-visible to VolumeInput tap-to-edit), this plan adds keyboard-only focus rings to 4 sub-picker / utility components. The PHASE 6 SPECIFIC change to EACH file is exactly ONE: a 5-token focus-visible ring class on the relevant interactive button(s).

The focus-visible ring spec (REUSED from Phase 5, per 06-UI-SPEC §"Focus-visible ring spec"):
```
focus-visible:outline-none
focus-visible:ring-2
focus-visible:ring-ipc-500
focus-visible:ring-offset-2
focus-visible:ring-offset-white
```

The 4 files and their NEW focus-visible additions:

1. **`DrinkTypePicker.tsx` (42 lines) — line 24 (each of 8 tiles)** — add focus-visible ring to the button className (per UI-SPEC §"DrinkTypePicker" table → "Focus ring" row, locked).

2. **`LeakTriggerPicker.tsx` (64 lines) — line 39 (each of 8 tiles)** — add focus-visible ring (per UI-SPEC §"LeakTriggerPicker" table → "Focus ring" row, locked). PRESERVE all 8 `data-testid="leak-trigger-${value}"` attributes (line 36 of the existing file).

3. **`SensationPicker.tsx` (84 lines) — TWO buttons get focus-visible:**
   - The help (?) toggle (line 30) — adds focus-visible
   - Each of 5 sensation pill buttons (line 47-51) — adds focus-visible (per UI-SPEC §"SensationPicker" table → "Focus ring" row + "Help (?) button" row, both locked)

4. **`VolumeInput.tsx` (127 lines) — line 98 (the tap-to-edit display button)** — add focus-visible to the className (per UI-SPEC §"Open Questions" → Q8 lock: "YES. Same focus-visible token (`focus-visible:ring-ipc-500`)").

**This plan does NOT change:**
- Any grid column count (DrinkTypePicker: `grid-cols-4`; LeakTriggerPicker: `grid-cols-4`; SensationPicker: `flex` with `flex-1`) — all preserved per UI-SPEC sub-picker tables
- Any tile sizing/min-height/padding
- Any color, background, border, ring (the selected-state visual indicator)
- Any aria-pressed, aria-label, onClick
- Any `data-testid` (LeakTriggerPicker's 8 testids strictly preserved)
- Volume preset chips in LogDrinkForm.tsx / LogVoidForm.tsx — those are inline in the form files (NOT separate components); Plan 06-05/06-06 already preserve them. The UI-SPEC notes them as candidates for focus-visible (§"Per-element additions" includes "Volume preset chips (×3 in LogDrink, ×3 in LogVoid)") but the planner DECIDES to NOT add them in this plan because: (a) Plans 06-05/06-06 explicitly state they do NOT modify the chip section and would conflict, (b) those chips' selected-state already shows `bg-drink` / `bg-ipc-500` — keyboard focus is implicitly indicated when the chip becomes the selected one. **Deferred to a Phase 8 polish item if needed.**
- LEAK_AMOUNT_OPTIONS buttons in LogLeakForm.tsx — same reasoning; the planner DEFERS to a Phase 8 polish item (or this plan can be amended at executor's discretion if the cross-AI review surfaces this as a blocker). **The UI-SPEC §"Per-element additions" listed these as candidates; the planner SCOPES them out because they would require Plan 06-07 to be edited or this plan to acquire LogLeakForm.tsx, both of which create file conflicts. Documented as Open Question 4 (residual).**
- Urgency Yes/No buttons in LogLeakForm.tsx — same reasoning, deferred.
- Cup-help toggle / note pills / leak pill / wokeBy buttons in the 3 forms — same reasoning. Deferred.
- Sticky Next button — already inherits focus-visible from Button primitive (Plan 06-03)
- Save buttons — already inherits from Button primitive

**Critical mobile invariant** per 06-UI-SPEC §"Mobile Invariants" → each picker row:
- `focus-visible:` triggers ONLY on keyboard focus, NOT on touch. Tap on a tile → no focus ring appears. This is the SAME UX improvement as the Button.tsx migration (Plan 06-03). **Zero pixel diff at mobile.**

**Critical data-testid preservation:**
- `leak-trigger-{cough,sneeze,laugh,lifting,exercise,toilet_way,other,not_sure}` (8 on LeakTriggerPicker line 36) — PRESERVE EXACTLY

Wave 4. Depends only on 06-03 (focus-visible pattern established in Button.tsx). Runs in parallel with Wave 3 (no file overlap; the 3 pickers and VolumeInput are separate files from the 5 forms).

Open questions addressed:
- Q4 (UI-SPEC) — QuickLogFAB speed-dial 40px → 44px bump: NOT addressed in this plan (would touch Phase 5's locked carve-out and require user confirmation per UI-SPEC Q4 — DEFERRED to a separate quick task post-Phase 6)
- Q8 (UI-SPEC) — VolumeInput tap-to-edit focus-visible: YES, addressed in this plan (Task 4)
- **Residual Open Question (new, planner-introduced; acknowledged in 2026-05-16 plan-check W2):** Inline buttons NOT covered by this plan (volume preset chips, LEAK_AMOUNT_OPTIONS, urgency Yes/No, cup-help toggle, leak/note pills, wokeBy buttons) — DEFERRED to **Phase 8 visual-qa audit** (which explicitly checks focus-ring visibility on every interactive form element and surfaces gaps as findings; NOT to ad-hoc "polish"). Rationale: file-conflict avoidance and execution scope budget. The deferred items meet the 44px hit-target floor and have visual selected-state indicators (color shift on click) that partially compensate for the missing focus-visible ring during keyboard navigation. Plan-checker W2 noted this weakens SC #7 ("Focus rings on all interactive elements"); the deferral is INTENTIONAL and the Phase 8 audit gate is the canonical follow-up. Phase 11 verification (Plan 06-11) ALSO spot-checks keyboard-walkthrough friction in EN + AR; any focus-gap findings get logged to Phase 6's CONTEXT.md `<deferred>` and batched into a follow-up quick task post-Phase-6.

Constraints from CLAUDE.md + memory:
- TypeScript strict
- All tests pass
- No em-dashes (no new copy)
- Mobile-pristine HARD CONSTRAINT
- All `data-testid` attributes preserved
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible ring to each DrinkTypePicker tile (8 buttons share one className expression — single string substitution)</name>

  <read_first>
    - src/components/diary/DrinkTypePicker.tsx (FULL 42 lines — verify the single map-rendered button at lines 20-37 with the className template literal at lines 24-29)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Sub-Picker Specs" → "DrinkTypePicker" table → "Focus ring" row (locked: new focus-visible 5-token spec on each tile)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Mobile Invariants" → DrinkTypePicker row
  </read_first>

  <files>src/components/diary/DrinkTypePicker.tsx</files>

  <action>
    Edit the className template literal at lines 24-29 of `src/components/diary/DrinkTypePicker.tsx`.

    Change FROM:
    ```
    className={`flex flex-col items-center gap-1.5 py-3 px-2 rounded-xl
      transition-all active:scale-[0.95] ${
        selected
          ? 'bg-drink text-white ring-2 ring-drink/30 shadow-sm'
          : 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm'
      }`}
    ```

    TO:
    ```
    className={`flex flex-col items-center gap-1.5 py-3 px-2 rounded-xl
      transition-all active:scale-[0.95]
      focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white
      ${
        selected
          ? 'bg-drink text-white ring-2 ring-drink/30 shadow-sm'
          : 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm'
      }`}
    ```

    The diff: insert ONE new line containing 5 `focus-visible:*` classes between `active:scale-[0.95]` and the ternary `${selected ? ...}`. The 5 tokens match Plan 06-03's spec and Phase 5's NavLink pattern.

    Do NOT touch:
    - The `DrinkIcon` rendering (lines 31-35)
    - The `<span>` label (line 36)
    - The outer grid wrapper (line 16) — `grid grid-cols-4 [grid-auto-rows:1fr] gap-2 mt-8` preserved
    - The `selected` / `key` / `type="button"` / `onClick` attributes

    Result: each of the 8 tiles (because the className is inside a `.map()` at line 17) gains the focus-visible ring.
  </action>

  <acceptance_criteria>
    - Source — focus-visible ring present: `grep -c 'focus-visible:ring-ipc-500' src/components/diary/DrinkTypePicker.tsx` returns at least `1`
    - Source — all 5 focus-visible tokens present: `grep -cE 'focus-visible:(outline-none|ring-2|ring-ipc-500|ring-offset-2|ring-offset-white)' src/components/diary/DrinkTypePicker.tsx` returns at least `5`
    - Source — grid unchanged: `grep -c 'grid grid-cols-4 \[grid-auto-rows:1fr\] gap-2 mt-8' src/components/diary/DrinkTypePicker.tsx` returns `1`
    - Source — file still 42-44 lines (the new className tokens add ONE logical line within a template literal): `wc -l < src/components/diary/DrinkTypePicker.tsx` returns a value in 42-44 inclusive
    - Source — no removal of existing classes: `grep -c 'bg-drink text-white ring-2 ring-drink/30' src/components/diary/DrinkTypePicker.tsx` returns `1` (selected-state preserved); `grep -c 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm' src/components/diary/DrinkTypePicker.tsx` returns `1` (resting state preserved)
    - Mobile-invariant: `git diff src/components/diary/DrinkTypePicker.tsx | grep '^+' | grep -vE 'focus-visible:|^\\+\\+\\+' | grep -E 'className=|sm:|md:|lg:|xl:|2xl:'` returns no matches (only focus-visible: classes added; no other responsive classes added; no className attributes added)
    - TypeScript clean: `npx tsc --noEmit` exits 0
    - Lint clean: `npx eslint src/components/diary/DrinkTypePicker.tsx` exits 0
  </acceptance_criteria>

  <done>
    DrinkTypePicker tiles (8 of them via map render) have focus-visible ring. Tap on mobile: no ring. Tab on desktop: ring appears. Grid columns, hit targets, all other classes preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add focus-visible ring to each LeakTriggerPicker tile (8 buttons); PRESERVE all 8 `data-testid="leak-trigger-*"` attributes</name>

  <read_first>
    - src/components/diary/LeakTriggerPicker.tsx (FULL 64 lines — verify the single map-rendered button at lines 33-52 with className at lines 39-44, AND the `data-testid={`leak-trigger-${lt.value}`}` at line 36)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Sub-Picker Specs" → "LeakTriggerPicker" table → "Focus ring" row (locked) + "data-testid" row (locked: preserve)
  </read_first>

  <files>src/components/diary/LeakTriggerPicker.tsx</files>

  <action>
    Edit the className template literal at lines 39-44 of `src/components/diary/LeakTriggerPicker.tsx`.

    Change FROM:
    ```
    className={`flex flex-col items-center justify-center gap-1.5 py-3 px-2 rounded-xl min-h-[76px]
      transition-all active:scale-[0.95] ${
        selected
          ? 'bg-leak text-white ring-2 ring-leak/30 shadow-sm'
          : 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm'
      }`}
    ```

    TO:
    ```
    className={`flex flex-col items-center justify-center gap-1.5 py-3 px-2 rounded-xl min-h-[76px]
      transition-all active:scale-[0.95]
      focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white
      ${
        selected
          ? 'bg-leak text-white ring-2 ring-leak/30 shadow-sm'
          : 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm'
      }`}
    ```

    PRESERVE EXACTLY:
    - `data-testid={`leak-trigger-${lt.value}`}` at line 36 — this is THE phrase the executor must NOT touch; the 8 testids are E2E walkthrough.spec.ts dependencies
    - `aria-pressed={selected}` at line 37
    - The outer `<div>` wrapping at line 26 + the grid at line 28
    - The description below the grid (lines 57-61)
  </action>

  <acceptance_criteria>
    - Source — focus-visible ring present: `grep -c 'focus-visible:ring-ipc-500' src/components/diary/LeakTriggerPicker.tsx` returns at least `1`
    - Source — all 5 focus-visible tokens present: `grep -cE 'focus-visible:(outline-none|ring-2|ring-ipc-500|ring-offset-2|ring-offset-white)' src/components/diary/LeakTriggerPicker.tsx` returns at least `5`
    - Source — `data-testid={\`leak-trigger-...\`}` present: `grep -c 'data-testid={`leak-trigger-' src/components/diary/LeakTriggerPicker.tsx` returns exactly `1` (the testid template literal in the map render — yields 8 unique testids at runtime)
    - Source — `min-h-[76px]` preserved: `grep -c 'min-h-\[76px\]' src/components/diary/LeakTriggerPicker.tsx` returns at least `1`
    - Source — selected/resting state classes preserved: `grep -c 'bg-leak text-white ring-2 ring-leak/30' src/components/diary/LeakTriggerPicker.tsx` returns `1`; `grep -c 'bg-white text-ipc-950 hover:bg-white border border-ipc-200/50 shadow-sm' src/components/diary/LeakTriggerPicker.tsx` returns `1`
    - Source — grid unchanged: `grep -c 'grid grid-cols-4 \[grid-auto-rows:1fr\] gap-2 mt-6' src/components/diary/LeakTriggerPicker.tsx` returns `1`
    - Mobile-invariant: `git diff src/components/diary/LeakTriggerPicker.tsx | grep '^+' | grep -vE 'focus-visible:|^\\+\\+\\+'` returns no additional class strings beyond focus-visible
    - TypeScript clean: `npx tsc --noEmit` exits 0
    - Lint clean: `npx eslint src/components/diary/LeakTriggerPicker.tsx` exits 0
  </acceptance_criteria>

  <done>
    LeakTriggerPicker tiles (8 of them) have focus-visible ring. All 8 `data-testid="leak-trigger-{value}"` preserved. `min-h-[76px]` preserved. Grid unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add focus-visible ring to SensationPicker — both the help (?) toggle (line 30) AND each of 5 pill buttons (lines 47-51)</name>

  <read_first>
    - src/components/diary/SensationPicker.tsx (FULL 84 lines — verify help button at lines 26-33, each pill button at lines 41-58)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Sub-Picker Specs" → "SensationPicker" table → "Focus ring" row + "Help (?) button" row (both locked)
  </read_first>

  <files>src/components/diary/SensationPicker.tsx</files>

  <action>
    Edit SensationPicker.tsx with the Edit tool in TWO places.

    **Edit A — Help (?) toggle button className (line 30):**

    Change FROM:
    ```
    className="text-ipc-400 hover:text-ipc-600 active:scale-[0.9] transition-all"
    ```

    TO:
    ```
    className="text-ipc-400 hover:text-ipc-600 active:scale-[0.9] transition-all
      focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white"
    ```

    **Edit B — Sensation pill button className (lines 47-51):**

    Change FROM:
    ```
    className={`flex-1 py-3.5 rounded-xl text-center transition-all
      active:scale-[0.95] min-h-[52px] ${
        selected
          ? 'bg-ipc-500/90 text-white font-bold'
          : 'bg-white/40 text-ipc-600 font-medium border border-ipc-100/50'
      }`}
    ```

    TO:
    ```
    className={`flex-1 py-3.5 rounded-xl text-center transition-all
      active:scale-[0.95] min-h-[52px]
      focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white
      ${
        selected
          ? 'bg-ipc-500/90 text-white font-bold'
          : 'bg-white/40 text-ipc-600 font-medium border border-ipc-100/50'
      }`}
    ```

    PRESERVE EXACTLY:
    - `aria-pressed={selected}` (line 45)
    - `aria-label={\`${s} ${t(\`${s}.short\`)}\`}` (line 46)
    - `flex-1` (load-bearing for the 5-equal-column layout)
    - `min-h-[52px]` (load-bearing for boomer-safe 44px floor + per-tile baseline)
    - The `flex gap-2 mt-2` parent wrapper (line 37)
    - The `space-y-2` outer wrapper (line 21)
    - Per-sensation description rendering (lines 64-68) and the help panel (lines 72-81)

    Result: 1 help (?) toggle + 5 pill buttons = 6 buttons gain focus-visible ring.
  </action>

  <acceptance_criteria>
    - Source — focus-visible ring present TWICE (help toggle + pill buttons): `grep -c 'focus-visible:ring-ipc-500' src/components/diary/SensationPicker.tsx` returns exactly `2`
    - Source — help toggle still has its className: `grep -c 'text-ipc-400 hover:text-ipc-600 active:scale-\[0.9\]' src/components/diary/SensationPicker.tsx` returns `1`
    - Source — pill button still has min-h: `grep -c 'min-h-\[52px\]' src/components/diary/SensationPicker.tsx` returns at least `1`
    - Source — selected/resting state preserved: `grep -c 'bg-ipc-500/90 text-white font-bold' src/components/diary/SensationPicker.tsx` returns `1`; `grep -c 'bg-white/40 text-ipc-600 font-medium border border-ipc-100/50' src/components/diary/SensationPicker.tsx` returns `1`
    - Source — flex gap-2 mt-2 preserved: `grep -c 'flex gap-2 mt-2' src/components/diary/SensationPicker.tsx` returns `1`
    - Mobile-invariant: `git diff src/components/diary/SensationPicker.tsx | grep '^+' | grep -vE 'focus-visible:|^\\+\\+\\+'` returns no additional non-focus-visible class strings
    - TypeScript clean: `npx tsc --noEmit` exits 0
    - Lint clean: `npx eslint src/components/diary/SensationPicker.tsx` exits 0
  </acceptance_criteria>

  <done>
    SensationPicker has focus-visible ring on the help (?) toggle (1 button) AND on each of 5 pill buttons. `flex-1` 5-column layout preserved. All aria attributes preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add focus-visible ring to VolumeInput tap-to-edit display button (line 98) — addresses UI-SPEC Open Question 8</name>

  <read_first>
    - src/components/ui/VolumeInput.tsx (FULL 127 lines — verify the tap-to-edit display button at lines 94-105, className at line 98)
    - .planning/phases/06-diary-forms-keyboard-navigation/06-UI-SPEC.md §"Open Questions" → Q8 (locked: YES, add focus-visible to VolumeInput tap-to-edit, line 98)
  </read_first>

  <files>src/components/ui/VolumeInput.tsx</files>

  <action>
    Edit the tap-to-edit button className at line 98 of `src/components/ui/VolumeInput.tsx`.

    Change FROM:
    ```
    className="inline-flex items-baseline gap-1 active:scale-[0.97] transition-transform"
    ```

    TO:
    ```
    className="inline-flex items-baseline gap-1 active:scale-[0.97] transition-transform
      focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ipc-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white rounded-lg"
    ```

    Note the `rounded-lg` added at the end: the button currently has NO border-radius (the `<span>` children are inline-baseline; the focus ring would render as a hard rectangular box around the natural inline-baseline button shape, which looks visually odd against the soft volume readout). Adding `rounded-lg` (6px) gives the focus ring a soft corner that matches the visual rhythm of the modal. This is a NET pixel diff on focus state only (no diff in resting/active state because no background, no border). Per UI-SPEC §"Volume readout typography bump" → "Decision: KEEP text-4xl at all breakpoints", the base shape of the button is unchanged.

    Do NOT modify the `<input type="text">` editing-mode style (lines 73-91) — its focus styling is its own (`focus:border-*` already exists at lines 85-88).

    Do NOT touch the slider `<input type="range">` (lines 110-122) — browser-native focus ring is preserved; UI-SPEC says "VolumeInput slider (input type='range') — gets browser-native focus ring; we override with the same spec for consistency" but the planner DEFERS this as a Phase 8 polish item because the browser-native ring is functional and consistent across all 6 locales; explicit override risks Tailwind specificity conflicts with the existing `volume-slider` / `volume-slider-night` / `volume-slider-drink` classes that own the range styling. Documented as Open Question 4 (residual; deferred to Phase 8).
  </action>

  <acceptance_criteria>
    - Source — focus-visible ring present on tap-to-edit button: `grep -c 'focus-visible:ring-ipc-500' src/components/ui/VolumeInput.tsx` returns at least `1`
    - Source — all 5 focus-visible tokens: `grep -cE 'focus-visible:(outline-none|ring-2|ring-ipc-500|ring-offset-2|ring-offset-white)' src/components/ui/VolumeInput.tsx` returns at least `5`
    - Source — rounded-lg added on the button (for focus-ring corner): `grep -c 'rounded-lg' src/components/ui/VolumeInput.tsx` returns at least `1` (NEW addition)
    - Source — existing inline-baseline gap-1 preserved: `grep -c 'inline-flex items-baseline gap-1' src/components/ui/VolumeInput.tsx` returns at least `2` (TWO: the editing-mode inline-flex AND the tap-to-edit button)
    - Source — slider <input type="range"> NOT modified: `grep -c 'volume-slider' src/components/ui/VolumeInput.tsx` returns at least `1` (existing class string preserved at line 117)
    - Source — editing-mode input className NOT modified: `grep -c 'border-2 rounded-xl py-1' src/components/ui/VolumeInput.tsx` returns at least `1` (the editing-mode input shape preserved)
    - Mobile-invariant: tap on the big number on touch → no focus ring appears (focus-visible doesn't trigger on tap). Tab to it on desktop → focus ring with soft corners.
    - TypeScript clean: `npx tsc --noEmit` exits 0
    - Lint clean: `npx eslint src/components/ui/VolumeInput.tsx` exits 0
    - Build (after all 4 tasks): `npx next build` exits 0
    - Vitest: `npx vitest run` exits 0
  </acceptance_criteria>

  <done>
    VolumeInput's tap-to-edit display button has focus-visible ring with rounded-lg corner. Slider and editing-mode input untouched. Mobile zero pixel diff in resting/active state.
  </done>
</task>

</tasks>

<must_haves>
  <truths>
    - All 8 DrinkTypePicker tiles have focus-visible ring
    - All 8 LeakTriggerPicker tiles have focus-visible ring; all 8 `data-testid="leak-trigger-*"` preserved
    - All 5 SensationPicker pill buttons + the help (?) toggle have focus-visible ring
    - VolumeInput tap-to-edit display button has focus-visible ring with soft `rounded-lg` corner
    - Mobile users on touch see NO focus ring after tapping any of these elements (focus-visible is keyboard-only)
    - Desktop Tab to any of these elements DOES show a clear ipc-500 ring with offset
    - Grid column counts and tile min-heights are preserved for all 3 pickers
    - VolumeInput slider and editing-mode input are NOT modified (preserved verbatim)
  </truths>
  <artifacts>
    - path: "src/components/diary/DrinkTypePicker.tsx"
      provides: "DrinkTypePicker with keyboard focus-visible rings on all 8 tiles"
      contains: "focus-visible:ring-ipc-500"
    - path: "src/components/diary/LeakTriggerPicker.tsx"
      provides: "LeakTriggerPicker with keyboard focus-visible rings on all 8 tiles + all 8 testids preserved"
      contains: "focus-visible:ring-ipc-500"
    - path: "src/components/diary/SensationPicker.tsx"
      provides: "SensationPicker with keyboard focus-visible rings on help toggle + all 5 pills"
      contains: "focus-visible:ring-ipc-500"
    - path: "src/components/ui/VolumeInput.tsx"
      provides: "VolumeInput tap-to-edit button with focus-visible ring"
      contains: "focus-visible:ring-ipc-500"
  </artifacts>
  <key_links>
    - from: "All 4 modified files (this plan)"
      to: "Plans 06-05/06/07/08 (the 5 forms that render these pickers + VolumeInput)"
      via: "These pickers are inline-rendered inside the forms; no API change needed"
      pattern: "When the BottomSheet's focus trap (Plan 06-04) cycles Tab through the form's children, the focus-visible ring naturally appears on each picker tile / pill / tap-to-edit button"
  </key_links>
</must_haves>

<verification>
After all 4 tasks complete:
```
for F in src/components/diary/DrinkTypePicker.tsx src/components/diary/LeakTriggerPicker.tsx src/components/diary/SensationPicker.tsx src/components/ui/VolumeInput.tsx; do
  echo "$F: $(grep -c 'focus-visible:ring-ipc-500' "$F") ring(s)"
done
```
MUST show: DrinkTypePicker 1, LeakTriggerPicker 1, SensationPicker 2, VolumeInput 1 — total 5 lines with non-zero counts (the count is # of className template literals containing the ring; SensationPicker has 2 because help toggle + pill button are separate template literals).

```
grep -c 'data-testid={`leak-trigger-' src/components/diary/LeakTriggerPicker.tsx
```
MUST return `1` (testid template literal preserved).

```
npx tsc --noEmit && npx eslint src/components/diary/ src/components/ui/VolumeInput.tsx && npx vitest run --silent 2>&1 | tail -3 && npx next build 2>&1 | tail -3
```
MUST exit 0 with successful build.
</verification>
</content>
